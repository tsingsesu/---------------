# 2026 高教社杯 C 题《微网与外部电网电力调控策略》题目解读

> 本文件由构思手在 Phase 0 完成，是全工作区对题目的唯一权威解读。
> 所有事实性数字均来自 `_phase0/` 下的体检脚本与报告，可复核：
> `_phase0/probe01_structure.py`→`报告01_文件结构.txt`；`probe02_profile.py`→`报告02_数据体检.txt`；
> `probe03_align.py`→`报告03_对齐与异常.txt`；`probe04_forecast_err.py`→`报告04_误差结构与基线.txt`；
> `probe05_template.py`、`_probe5b.py`、`_p5c.py`、`_p5d.py`→`报告05`~`报告10`。

---

## 零、一句话总览

题目是一个**"光伏 + 储能 + 外部电网"的小型发用电系统的购电费用最小化问题**，四问沿着"确定性单日 → 多日滚动 → 预报滚动的偏差结算 → 电价也波动"逐层加难度：

| 问 | 不确定性来源 | 时间跨度 | 核心机制 | 决策对象 |
|---|---|---|---|---|
| 1 | 无（给定一天的预测） | 1 天（144 个 10 分钟时段） | 储能 0:00 与 24:00 储电量相同 | 计划购电量 + 充放电量 |
| 2 | 题目未给预报 → 见 §7 冲突 C-3 | 2025.2.1–12.31（334 天） | 缺电时紧急购电，电价 ×5 | 每日 0:00 的计划购电量 + 充放电量 |
| 3 | 光伏预报（0:00/6:00/12:00/18:00 滚动发布） | 同上 | 偏差结算：计划 > 调整按 0.5×，调整 > 计划按 1.5×；外加紧急购电 ×5 | 计划购电量 + 各预报时刻的调整购电量 |
| 4 | 光伏预报 + 电价逐日波动 | 同上 | 用附件 4 的波动电价重做问题 2、问题 3 | 同问题 2、3 |

**贯穿四问的物理内核只有一个**：储能设备在时间上搬运电能。四问共用同一套储能参数、同一套 10 分钟时间粒度、同一套符号（见 `符号表.md`）。问题 1 是问题 2/3/4 的退化情形，代码与公式必须复用而不是重写。

---

## 一、题目与数据的全局事实（Phase 0 实测结论）

### 1.1 数据清单

| 文件 | 结构 | 单位 | 量级 |
|---|---|---|---|
| 附件1 | 1 张表，**144 行**（时间 0:10 … 0:00+1）× 4 列：时间 / 电价 / 小区负载 / 光伏发电预测功率 | 元·kWh⁻¹ / kW / kW | 电价 0.3713–1.3952；负载 3309.39–5958.97；光伏 0–7612.32 |
| 附件2 | **2 张工作表**：`小区负载`、`光伏发电实际功率`；各 **365 行 × 144 列**（行为 2025.1.1–12.31，列为 0:10 … 0:00+1） | kW | 负载 1995.72–7978.88；光伏 0–10216.20 |
| 附件3 | 1 张表，**1460 行**（365 天 × 4 个发布时刻）× 26 列：日期 / 预报时刻 / 预报1小时 … 预报24小时；"日期"列按天合并 | kW | 0–9995.89 |
| 附件4 | 1 张表，**365 行 × 144 列**，电价 | 元·kWh⁻¹ | 0.0076–1.7936 |
| 附件5 | 5 个结果模板（见 §1.6、各问"输出要求"） | — | — |

- **无缺失**：附件1/2/3/4 全部单元格均有数值，NaN 计数为 0；无负值（电价与光伏均 ≥0）。
- 附件2 全年：负载电量 **4052.4 万 kWh**，光伏电量 **2025.1 万 kWh**，光伏/负载 ≈ 0.50；逐日光伏/负载比在 0.2707–0.9990 之间（**没有全阴天**，最小的一天光伏仍占负载 27%）。
- 附件2 负载的极端值：最小 1995.72 kW、最大 7978.88 kW；附件2 光伏峰值 10216.20 kW。

### 1.2 时间标签约定（**全题最容易做错的地方**）

- 附件2、附件4 的列表头是 `0:10, 0:20, …, 23:50, 0:00+1`，共 **144 个**时间标签；附件1 的行标签同构。
- 附录2 明确说明"表中的 0:00+1 表示第二天凌晨 0:00，即当天 24:00"。据此，**这 144 个标签是"时段右端点"**：第 $k$ 个值代表时段 $(10(k{-}1)\text{min},\,10k\text{min}]$ 的平均量，144 个时段恰好铺满 $[0{:}00,\,24{:}00]$。
- **但附件5 的模板把 144 个时段标签整体右移了一格**：result1 的 `计划购电量` 首行为 `0:10-0:20`、末行为 `0:00+1-0:10+1`；result2/3/4 的时段列同理（首列 `0:10-0:20`，末列 `0:00-0:10+1`）。也就是说模板中**不存在** `0:00-0:10` 这一格，却多出一格 `24:00-24:10`。
- 可能的填法有三种，后果不同：
  - **填法 X（逐位置对应）**：把附件1/2/4 的第 $k$ 个数据值填进模板第 $k$ 个格子。144 格恰好覆盖 $[0{:}00,24{:}00]$，全天量自洽；但模板中标签为 `10:00-10:10` 的格子，其物理含义是 $9{:}50$–$10{:}00$，**标签与内容错位**。
  - **填法 Y-literal（模板字面）**：让标签字面成立，则整行覆盖 $0{:}10$–$24{:}10$，少了当天 $0{:}00$–$0{:}10$、多了次日 $0{:}00$–$0{:}10$；而且 **2025-12-31 那一行没有次日数据可用**，必须回退，无法全程一致。
  - **填法 Y-轮转（已采用，见 `口径与假设台账.md` D-01）**：模板第 $k$ 格（$k=1,\dots,143$，标签 $(10k\min,10(k{+}1)\min)$）填**当天第 $k{+}1$ 个时段**；**第 144 格**（标签 `0:00+1-0:10+1`）填**当天第 1 个时段**（$0{:}00$–$0{:}10$）。
- **已定裁定（用户团队确认 + 构思手细化）：采用填法 Y-轮转**。理由：(i) 模板标签与格的物理含义**逐格对上**（表 1 的 `10:00-10:10` 与模板同标签格子完全一致），避免"标签与内容错位"这种会被直接看出来的问题；(ii) 轮转后行内 144 格**恰好覆盖当天 $[0{:}00,24{:}00]$ 一次且仅一次**，`全天购电量`/`全天购电费` 仍等于当天总量，"总是自洽"；(iii) 12-31 无次日数据这一硬约束，把 Y-literal 排除掉，而 Y-轮转对任何一天都可用。
- **因轮转带来的两条操作纪律**（必须写进代码与文档）：
  1. **模板第 144 格在时间上是最早的一段**。一切聚合（表 2 的六段、图表曲线、全天汇总）必须按**真实时间顺序** $j=1,\dots,144$，**不得按模板列序**；绘图时要把第 144 格接回序列开头。
  2. 表 1 的六个指定时段在模板中的位置：`H:00-H:10` ↔ 当天第 $6H{+}1$ 个时段（模板第 $6H$ 格）。
- 该约定**必须在每问 `.md` 与论文表注中显式说明**（`AGENTS.md` §1.8 的强制要求）；稳健性实验中给出填法 X 下关键总量的差异。
- 附件3 的 `预报m小时`：实测其数值与附件2 中**标签为 $(发布时刻+m){:}00$ 的那个 10 分钟区间值**高度吻合（详见 §1.4），故约定 `预报m小时` 指整点 $(\tau+m){:}00$ 处的光伏出力，其覆盖的时段是 $(\tau{+}m{-}1,\,\tau{+}m]$。24 条预报恰好覆盖未来 24 小时。

### 1.3 附件1 是"全年典型日"（实测，非猜测）

逐列比对（144 列，列 = 时刻）：

| 比对 | 最大绝对偏差 |
|---|---|
| 附件1 电价 vs 附件4 电价的**逐列均值** | 5.2×10⁻⁵ |
| 附件1 负载 vs 附件2 负载的**逐列均值** | 5.0×10⁻⁵ |
| 附件1 光伏 vs 附件2 光伏的**逐列均值** | 4.3×10⁻² |
| 附件1 电价均值 vs 附件4 电价均值 | 0.766197 vs 0.766198 |

**结论**：附件1 的三列就是全年 365 天逐时刻的平均曲线（典型日）。这直接落实了问题 1 的"每天的电价和小区负载相同"：问题 1 就是在这条典型日曲线上做单日优化。附件1 的光伏列名为"光伏发电预测功率"，即典型日的功率预测，问题 1 直接使用；附件2 的光伏列名为"实际功率"，是问题 2/3/4 的实际出力。

**典型日基线（问题 1 的对标值）**：

| 量 | 数值 |
|---|---|
| 负载电量 | 111 024.81 kWh |
| 光伏电量 | 55 482.84 kWh（占负载 50.03%） |
| 净负载电量 | 55 541.97 kWh |
| 方案 A：全部向电网购电（不储能、不弃光）费用 | 88 319.36 元 |
| 方案 B：光伏优先自用、余电弃掉（不储能）费用 | 48 052.05 元 |
| 储能的边际价值空间 | 峰谷价比 1.3952/0.3713 = **3.758** |
| 套利可行性门槛 | 往返效率 0.81 ⇒ 需价比 > 1.235；实际 3.758 ≫ 1.235，**套利可行且空间显著** |

### 1.4 附件3 的光伏预报：时间对齐与误差结构（实测）

对 `预报m小时` 与附件2 实际值做了 4 种对齐读法的全样比对（35 000 个格点，4 个发布时刻）：

| 读法 | 说明 | MAE | RMSE |
|---|---|---|---|
| A | 预报 m 小时 = 小时块 $(m{-}1,m]$ 的均值 | 340.6 | 563.7 |
| B | 预报 m 小时 = 小时块 $[m,m{+}1)$ 的均值 | 427.7 | 679.3 |
| **C（最优）** | **预报 m 小时 = 标签 $(发布时刻{+}m){:}00$ 的那个 10 分钟点的实际值** | **181.3** | **370.6** |
| D | 预报 m 小时 = $(m{+}1){:}00$ 处的实际值 | 673.4 | 1044.7 |

分发布时刻的读法 C：0:00 → MAE 181.3 / RMSE 370.6；6:00 → 97.5 / 206.8；12:00 → 207.9 / 561.0；18:00 → 259.9 / 524.2。并且 **50.47% 的格点与整点实际值完全相等（差 < 10⁻⁹）**，非零偏差的中位数为 −1.11 kW、标准差 620.07 kW，预报/实际比值的中位数为 0.9983、四分位区间 0.9388–1.0515。

**整点预报如何落到 144 个 10 分钟区间上（已做判定实验，见 `_phase0/probe07_disaggregate.py`、`报告12_整点预报分解判定.txt`）**

先把"整点值"的结构查清楚：对附件2 光伏，以 $s$ 列为一段、段首 $A$、段末 $B$，统计归一化位置 $r_m=(x_{c_0+m}-A)/(B-A)$。取 $s=6$（小时结点）时，$r_m$ 的均值 = 0.1646 / 0.3342 / 0.5019 / 0.6692 / 0.8373，而线性插值的理论值是 0.1667 / 0.3333 / 0.5000 / 0.6667 / 0.8333——**光伏在小时内平均就是近似线性的**（负载与电价则不是：其 $r_m$ 均值为 0.107/0.199/0.117/0.371/0.879 与 0.098/0.116/0.074/0.396/0.915，呈"先平后跳"的台阶状，因此负载与电价直接使用 10 分钟原始值，不做任何插值）。

据此对四种分解方式做了全样精度对比（365 天、0:00 预报覆盖全天）：

| 方法 | MAE(白天) | RMSE(白天) | RMSE(全样本) | RMSE(非整点列) |
|---|---|---|---|---|
| M1 整点值覆盖整小时（分段常数） | 723.7 | 906.0 | 636.4 | 677.1 |
| **M2 相邻整点线性插值（已采用）** | **387.3** | **538.4** | **374.5** | **375.3** |
| M3 整点值 × 历史小时内形状 | 727.2 | 910.4 | 639.4 | 680.5 |
| M4 整点值 × 典型日小时内形状 | 727.0 | 909.4 | 638.7 | 679.8 |

纯整点预报误差为 MAE 181.3 / RMSE 370.6，而 **M2 的全样本 RMSE 374.5 只比它高 1%**——线性插值几乎不引入额外误差；M1/M3/M4 则把误差抬高 70% 以上。

**对建模的三条直接含义**：

1. 预报只给到**整点、24 个值**，而计划需要 **144 个 10 分钟值**，因此必须做**分解（disaggregation）**。**已定方法：以当天整点 $0{:}00,1{:}00,\dots,24{:}00$ 为结点做线性插值**（当天 $0{:}00$ 这个结点取自**前一日的 0:00 预报的第 24 个值**；2025-01-01 无前一日，该时段为夜间取 0）。该结论有实测支撑（见上表），并且是"用整个小时内实测形状缩放"这类做法无法比拟的。把 M1 与"小时粒度 LP"作为灵敏度对照，量化粗化代价。
2. 预报误差是**乘性、相对误差约 ±6%（四分位）**、且约一半格点无误差的结构；误差随发布时刻与时段变化（6:00 的预报最准，因为它对白天时段的提前期最短）。
3. 误差量级（RMSE 数百 kW，相对白天出力 5000–10000 kW 约 5%–10%）决定了问题 3 的紧急购电量**不会很大但也不会恒为零**——这正是表 3 在问题 3 中有意义的原因。
4. **一个待数值验证的推论（Phase 1 的实验项，不是已证结论）**：把问题 3 的单时段费用写成计划量 $x$ 的函数（设该时段最终需求为 $R$、且调整量能完全覆盖需求，即 $y=R$）：
   $$J(x)=\begin{cases} 1.5pR-0.5px, & x\le R\\[2pt] 0.5px+0.5pR, & x>R\end{cases}$$
   即 $x$ 低于需求时边际斜率为 $-0.5p$、高于需求时为 $+0.5p$。令 $\mathrm{d}\,\mathbb{E}[J]/\mathrm{d}x=0$ 得 $P(R<x)=P(R>x)$，**最优计划量是未来需求分布的中位数**——计划阶段不该锚定预报的条件期望，而应由"需求超过 $x$ 的概率"与"低于 $x$ 的概率"的平衡决定；紧急购电（$5p$）的尾部会把最优分位**向中位数之上推**，而完全不能调整时（问题 2 的结构）斜率在 $x<R$ 时为 $-4p$，最优计划会趋向需求的上极端。这一串结论（最优分位水平、调整能力对锚点的影响）应当用数值实验落实后再写进论文，不能只作断言。

### 1.5 附件4 电价的结构（实测，是问题 4 的关键）

- 逐点比值 $p^{\text{附件4}}/p^{\text{附件1}}$ 的**逐列（逐时刻）均值为 1.0000**（范围 0.9999–1.0001）。
- 逐日比值均值的分布：均值 1.0000、标准差 0.1401、范围 **0.6876–1.2132**；逐点比值标准差 0.1798；取对数后标准差 0.20197；乘性日因子模型的相对残差均值 9.6%。
- **结论：附件4 的电价 = 附件1 的典型日形状 × 一个逐日乘性因子（0.69–1.21）× 约 ±10% 的日内噪声。日内峰谷结构每天相同，跨天的差异只有 ±20% 量级。**
- 同理，附件2 负载的逐列比值均值也恒为 1.0000，逐点比值分位 (0.426, 0.541, 1.059, 1.379, 1.575) ⇒ **负载也是"典型日形状 × 逐日乘性因子"**。
- **这条结构结论有重要的建模后果**：日内峰谷价差 3.758 倍，而跨天价差最多约 1.21/0.69 = 1.75 倍（且还要乘往返效率 0.81 才能套利，需 1.235 倍门槛）。因此**跨天套利的收益远小于日内套利**，这为"逐日滚动优化"提供了定量依据，也是问题 4 可以直接沿用问题 2/3 框架的理论支撑（但仍须做跨天耦合的稳健性检验）。

### 1.6 附件5 结果模板的逐条规定（原样抄录）

| 文件 | 工作表 | 精确规格 |
|---|---|---|
| `result1.xlsx` | `计划购电量` | 2 列：`时间段` / `购电量`；**144 个数据行**，标签 `0:10-0:20` … `23:50-0:00+1`、`0:00+1-0:10+1`。`购电量` 列的数字格式模板设为 **`0.0000`**（小数 4 位） |
| `result1.xlsx` | `充放电量` | 5 列：`时间段` / `充电量` / `放电量` / `时刻` / `储电量`；6 个数据行：`0:00-4:00`、`4:00-8:00`、`8:00-12:00`、`12:00-16:00`、`16:00-20:00`、`20:00-24:00`；`时刻` 列在第 1、2 行分别为 `0:00`、`24:00` |
| `result2.xlsx` | `计划购电量` | **335 行 × 147 列**：第 1 列 `日期\时间`（预填 2025-02-01 … 2025-12-31，共 334 天）+ **144 个时段列**（`0:10-0:20` … `23:50-0:00+1`、`0:00-0:10+1`）+ `全天购电量` + `全天购电费` |
| `result2.xlsx` | `充放电量` | 6 列：`日期` / `时间段` / `充电量` / `放电量` / `时刻` / `储电量`；模板只给了 **19 个示例数据行（3 天 + 1 个时段）**，**必须自行扩到 334 天 × 6 时段 = 2004 行**；`时刻` 列每天第 1 行填 `0:00`、第 2 行填 `24:00` |
| `result2.xlsx` | `紧急购电量` | 3 列：`日期` / `购电时间段` / `购电量`；模板缩略给出 `2025-02-01`(3 行)、`2025-02-02`(3 行)、省略行 `⁝`、`2025-12-31`(3 行)。填法严格按**表 4 示例**：一行一天，多个时段用空格分隔，多个电量用空格分隔、与时序一一对应 |
| `result3.xlsx` | 4 张表 | `计划购电量`（335×147，同 result2）、`调整购电量`（335×147，同构，含 `全天购电量`/`全天购电费` 列）、`充放电量`（同 result2，需扩到 2004 行）、`紧急购电量`（同 result2） |
| `result4-2.xlsx` | 3 张表 | `计划购电量`、`充放电量`、`紧急购电量`（结构同 result2） |
| `result4-3.xlsx` | 4 张表 | 结构同 result3 |

**模板中的两个"陷阱"**：
1. `充放电量`、`紧急购电量` 只给了示例行，**不是全年模板**，必须自行扩表到 334 天，否则"保存 2025.2.1-12.31 的充放电量"这一要求未满足。
2. 时段标签的错位问题见 §1.2；模板中还有一处笔误：result2/3/4 的第 42 个时段列标签写作 `7:0-7:10`（少一个 0），**照抄模板原文**，不改。

### 1.7 储能与算例规模

- 参数（附录1）：额定容量 $C=12000$ kWh；最大充放电功率 $\bar P=5000$ kW；储电量允许区间 $[\underline E,\bar E]=[1200,\,10800]$ kWh（可用 9600 kWh）；充放电效率 90%；2025-01-01 0:00 储电量 $E_0=6000$ kWh。
- 时段换算：10 分钟 = 1/6 h，故单时段最大充电量 = 单时段最大放电量 = $5000\times\frac16=833.333$ kWh。
- 满功率把 9600 kWh 从下限充到上限需 1.92 h ⇒ 储能**在时间上足够灵活**，日内两次套利循环（早高峰 + 晚高峰）都做得到。
- 算例规模：问题 1 是 144 时段 × 3 类决策变量 ≈ 432 变量的线性规划（LP）；问题 2/3/4 是 334 天规模，逐日 LP 共 334 个 3×144 维小 LP（问题 3 还要在每天内再分 4 个决策时刻），**用 `scipy.optimize.linprog`（HiGHS）逐日求解即可，无需外部求解器**；另需一条"全年联合 LP"作为上界对照（变量约 15.8 万，稀疏，HiGHS 可解但耗时较长，作为离线对照实验）。

### 1.8 异常值与数据说明（必须在论文里交代的）

- 附件4 电价有 **9 个低于 0.05 元/kWh 的点**，集中在 4 天：2025-04-26（4 个）、2025-05-30（2 个）、2025-09-13（2 个）、2025-10-10（1 个），最小值 0.0076 元/kWh。没有 ≤0 的值。这些点保留（题目未说明可剔除），但在稳健性实验中做"剔除极端低价点后重算"的对照。
- 附件2 光伏在夜间有大量非零小值（<10 kW 共 2678 个，占全样本 5.10%），白天时段（6:00–18:00 对应列）也出现 945 个 0。这些是数值噪声，量级相对白天出力（数千 kW）可忽略，**不清洗、不插补**，但在数据侧扰动实验中做"小于 10 kW 置零"的对照。
- 数据的小数位模式显示负载/电价/光伏并非简单线性插值生成（逐时刻二阶差分在小时间界仅比小时内部大 1.3–2.6 倍），**10 分钟粒度携带真实变化，不应粗化到小时**。但仍安排"小时粒度重算"作为方法侧稳健性对照。

### 1.9 问题 1 主模型的量级体检（已实跑一次 LP，用于锚定验收区间）

为免验收标准里出现"拍脑袋"的数，构思手先用 `_phase0/probe06_q1_scale.py` 把问题 1 的主模型跑通了一遍（LP 用 `scipy.optimize.linprog`/HiGHS，432 个变量、432 条约束，瞬时求解），结果见 `_phase0/报告11_问题1量级体检.txt`：

| 量 | 主模型 $E_0=E_{144}=6000$ | 对照：端点自由 |
|---|---|---|
| 全天购电量 | **59 482.6990 kWh** | 54 149.3657 kWh |
| 全天购电费 | **35 126.9486 元** | 32 909.8653 元 |
| 充电量合计 / 放电量合计 | 20 740.67 / 16 799.94 kWh（比值 0.8100 = $\eta^2$，验证效率口径写对） | 15 407.33 / 16 799.94 kWh |
| 储电量轨迹 | 1200 → 10800 → 6000（上下限均触及） | 6000 → 10800 → 1200 |
| 购电量为 0 的时段数 | 58 / 144 | 58 / 144 |
| 同时充放的时段数 | **0**（验证"$\eta^2<1$ 时存在 $uv=0$ 的最优解"这条引理） | 0 |

- 相对"方案 B 光伏自用不储能 48 052.05 元"，主模型节省 **12 925.10 元（26.90%）**；端点自由只再省 2 217.08 元（6.31%）⇒ 端点口径确实影响结果，论文中必须两套都报（见 `口径与假设台账.md` D-03）。
- 效率口径影响：单向 0.9（往返 0.81）→ 32 909.87 元；往返 0.9（单向 0.9487）→ 31 695.89 元，差 3.7% ⇒ 论文中必须两套都报（D-05）。
- 表 1 六个指定时段在**填法 Y-轮转**（已定口径，D-01）下的购电量：

  | 时段 | 购电量 (kWh) | 对应当天第几个时段 | 数据列（0 基） |
  |---|---|---|---|
  | 10:00-10:10 | 0.0000 | 第 61 个 | 60 |
  | 12:00-12:10 | 480.4124 | 第 73 个 | 72 |
  | 14:00-14:10 | 0.0000 | 第 85 个 | 84 |
  | 16:00-16:10 | 445.4317 | 第 97 个 | 96 |
  | 18:00-18:10 | 531.8940 | 第 109 个 | 108 |
  | 20:00-20:10 | 0.0000 | 第 121 个 | 120 |

  **注意错位的量级**：若按已被否决的填法 X（逐位置对应）取数，18:00-18:10 会得到 636.9826 kWh，与正确值 531.8940 相差 20%——这从数值上说明 §1.2 的口径必须显式确定并写进论文，不能"随便填"。
- 因此各问验收区间锚定为：问题 1 全天购电量 5.4×10⁴–6.0×10⁴ kWh、购电费 3.2×10⁴–3.6×10⁴ 元；任何偏离该区间的结果都必须给出解释。

---

## 二、问题 1

### 2.1 问的是什么

**题目原文关键句**：
> "若每天的电价和小区负载相同，同时给出光伏发电功率一天的预测数据，要求微网提供的电能不可低于小区负载，且储能设备在 0:00 和 24:00 的储电量相同。请在每天 0:00 制定微网当天的计划购电策略的数学模型。"
> "根据附件 1 和附录 1 中的数据，给出具体的计划购电策略，在论文中以表 1 的格式给出指定时间段的购电量（单位：kWh）及全天的购电量（单位：kWh）和购电费（单位：元），以表 2 的格式给出储能设备在指定时间段的充放电量及 0:00 和 24:00 的储电量（单位：kWh），并将完整的计划购电策略保存到文件 result1.xlsx 中（模板文件见附件 5）。"

**用自己的话复述**：电价、负载每天完全一样，光伏出力也已知（都是附件1 的典型日曲线）。在 0:00 一次性决定当天 144 个 10 分钟时段各买多少电、储能怎么充放，使得全天的购电费最小。硬约束只有两条：任何时刻微网供给的电能不低于负载；储能 0:00 与 24:00 储电量相同。**这是一个完全信息、单日、带储能状态约束的确定性线性规划。**

### 2.2 已知 / 未知 / 决策变量 / 约束 / 输出要求

- **已知**：附件1 的 $p_k$（电价，元/kWh）、$L_k$（负载，kW）、$P_k$（光伏预测功率，kW），$k=1,\dots,144$；附录1 的储能参数与 $E_0=6000$ kWh。
- **未知/决策变量**：计划购电量 $x_k\ge 0$（kWh，时段 $k$ 内）；储能充电量 $u_k\ge 0$、放电量 $v_k\ge 0$（kWh）；状态变量储电量 $E_k$（kWh）。
- **约束**：
  1. 功率平衡/供给非欠：$x_k + P_k\Delta + v_k \;\ge\; L_k\Delta + u_k$，其中 $\Delta=\frac16$ h（超出的光伏可弃光，$x_k$ 不会为负）。
  2. 充放电功率上限：$0\le u_k\le \bar P\Delta$，$0\le v_k\le \bar P\Delta$（833.333 kWh）。
  3. 储能状态方程：$E_k = E_{k-1} + \eta u_k - v_k/\eta$，$\eta=0.9$（**效率口径见 §7 冲突 C-4，这是待用户拍板项**）。
  4. 储电量边界：$\underline E \le E_k \le \bar E$，$k=1,\dots,144$。
  5. 首末储电量：$E_0 = E_{144}$，且（推荐）$E_0=E_{144}=6000$ kWh。
  6. 非负：$x_k,u_k,v_k\ge 0$。
- **目标**：$\min \sum_{k=1}^{144} p_k x_k$。
- **输出要求**：
  - 论文**表 1**：指定 6 个时段的购电量 + 全天购电量（kWh）+ 全天购电费（元）。表 1 的 6 个时段为 `10:00-10:10`、`12:00-12:10`、`14:00-14:10`、`16:00-16:10`、`18:00-18:10`、`20:00-20:10`（PDF 表格排版错乱，按此重构）。
  - 论文**表 2**：`0:00-4:00`、`4:00-8:00`、`8:00-12:00`、`12:00-16:00`、`16:00-20:00`、`20:00-24:00` 六个时段的充电量与放电量（kWh），以及 `0:00` 储电量、`24:00` 储电量（kWh）。
  - 文件 `result1.xlsx`：严格用附件5 的模板，两张工作表，规格见 §1.6。

### 2.3 重难点

1. **效率的方向性**：充电时 $E$ 增加 $\eta u$，放电时 $E$ 减少 $v/\eta$。写成 $E_k=E_{k-1}+\eta u_k - v_k/\eta$ 与写成 $+\eta u_k-\eta v_k$ 是两种不同口径，结果会差 10% 以上。这是本题第一大坑。
2. **首末储电量到底是不是 6000**：题目只说"0:00 和 24:00 的储电量相同"，附录1 说"2025 年 1 月 1 日 0:00 的电量为 6000 kWh"。见 §7 冲突 C-2，**推荐取 $E_0=E_{144}=6000$**，同时给出"端点自由（周期最优）"的对照解。
3. **单位换算**：负载/光伏给的是 **kW**，购电量/充放电量要的是 **kWh**，必须乘 $\Delta=1/6$。全天购电量 ≈ 5.6 万 kWh 量级，若忘记乘 $\Delta$ 会得到约 6.7 万的错数。
4. **同时充放**：LP 松弛下 $u_k$ 与 $v_k$ 可能同时为正。可证明当 $\eta^2<1$ 时同时充放严格劣，存在一组最优解满足 $u_kv_k=0$，需在论文中给出这条引理（也解释了为什么不需要引入整数变量）。
5. **弃光**：光伏富余时既不卖电也不强制储存，模型里必须允许"少用"（供给不等式而非等式）。若把平衡写成等式 $\text{供给}=\text{负载}$，会在光伏富余时段造成无解。
6. **表格填法**：见 §1.2 的**填法 Y-轮转**（第 144 格装当天第 1 个时段；聚合与绘图必须按真实时间顺序）。全天购电量必须与表内 144 个值的和一致。

### 2.4 得分点

> **得分点**：把"微网供给不低于负载"写成不等式（允许弃光）；把"0:00 与 24:00 储电量相同"写成对首末状态的显式约束并说明其含义（日周期运行）；给出效率的双向口径与状态递推的完整推导。
> **得分点**：给出全天购电量与购电费的**分解**：不储能基线 48 052.05 元 → 优化后的费用 → 节省额与节省率，并解释储能的"净购电量反而增加"现象（往返效率 0.81 使循环本身耗能），这体现对模型的理解深度。
> **得分点**：报出 6 个指定时段的购电量并解释其与电价、光伏的对应关系（例如 `10:00-10:10` 与 `12:00-12:10` 落在光伏富余、电价低谷段，购电量应显著偏低）。
> **易错点**：漏乘 $\Delta$；$E_0$ 取 0 或取容量；用等式而非不等式写平衡；把"充电量/放电量"直接以 kW 填进 kWh 的表。

### 2.5 候选模型

| 候选 | 内容 | 适用性/实现难度/风险 | 评价 |
|---|---|---|---|
| **M1 线性规划（LP）** | 变量 $x,u,v,E$，约束如上，`scipy.optimize.linprog`（HiGHS）精确求解 | 完全适配；实现简单；解是最优解，可复算 | **选型结论：主模型** |
| M2 动态规划（DP） | 以 $E_k$ 为状态、$k$ 为阶段做 DP，状态离散化 | 适配且能自然处理非线性；但 144 阶段 × 9600 kWh 状态离散化，精度受网格限制 | **作为方法侧交叉验证**：用粗网格 DP 复核 LP 的总费用与 SOC 轨迹量级 |
| M3 规则式策略（谷充峰放阈值策略） | 设价格阈值 $\theta_1<\theta_2$，$p<\theta_1$ 则满功率充、$p>\theta_2$ 则满功率放 | 实现极简；但阈值需调参，无法保证最优 | **作为对照基线**，用于量化"优化相对规则策略的增益" |

**选型结论：以 M1 为主模型，M2 做方法侧稳健性互验，M3 做基线对照。** 理由：题目是标准线性结构，LP 能给出全局最优与对偶信息（边际购电成本），对偶变量还可用于解释"为什么某些时段购电量为 0"，这是拿分且有深度的做法；DP 与 LP 的差异本身就是一个很好的稳健性证据。

### 2.6 与其它问的耦合

- 问题 1 的解是问题 2/3/4 的**退化基线**：把"$E_0=E_{144}$、典型日数据"换成"逐日传递的储电量、逐日数据"即可复用同一套 LP 构造代码（建议放在工作区根目录 `lib/`）。
- 问题 1 的典型日曲线就是全年数据求均值得到的（§1.3 已证），因此问题 1 的结果可以直接与问题 2 的"全年平均单日费用"对照——**这两个数字应当接近**，是一个免费的自洽性检验。
- 符号 $x,u,v,E,p,L,P,\eta$ 在四问中全部复用，不新增同义符号。

### 2.7 待确认事项（需用户拍板）

见 §7 冲突清单中的 C-2（$E_0$ 是否锁定 6000）与 C-4（效率口径）。其余无阻塞。

---

## 三、问题 2

### 3.1 问的是什么

**题目原文关键句**：
> "若每天的电价相同，而小区负载和光伏发电功率随时间变化，且微网提供的电能不可低于小区负载，如果低于负载，需向外网紧急购电，其电价是交易时刻电价的 5 倍。除紧急购电费用外，其他时间段的购电费用均按计划购电量计算。请在每天 0:00 制定微网当天的计划购电策略。"
> "根据附件 1 中的电价和附件 2 中的数据，在每天 0:00 制定当天的计划购电策略。在论文中按表 1 和表 2 的格式给出表 3 中指定日期的结果，按表 3 的格式给出紧急购电的结果，并将完整的购电策略保存到文件 result2.xlsx 中（模板文件见附件 5）。"

**用自己的话复述**：电价仍取附件1 的典型日曲线（每天相同），但负载与光伏逐日逐时段变化（附件2 的 365 天数据）。每天 0:00 为该天制定一次"计划购电量 + 储能充放电"方案。计划一旦定下，购电费按**计划量**结算；如果实际供给仍低于负载，缺的那部分按**当刻电价的 5 倍**紧急购电。要求对 2025.2.1–12.31 共 334 天逐日给出策略。

### 3.2 已知 / 未知 / 决策变量 / 约束 / 输出要求

- **已知**：$p_k$（附件1，对每一天相同）；$L_k^d$、$P_k^d$（附件2，$d=1,\dots,365$ 对应 2025-01-01…12-31）；储能参数与 $E_0=6000$ kWh（2025-01-01 0:00）。
- **决策变量**：$x_k^d$（计划购电量）、$u_k^d$、$v_k^d$；状态 $E_k^d$；以及紧急购电量 $r_k^d$（由约束决定的缺额，非独立优化对象）。
- **约束**：
  1. 供给充足：$x_k^d + P_k^d\Delta + v_k^d + r_k^d \;\ge\; L_k^d\Delta + u_k^d$；等价地 $r_k^d=\big[L_k^d\Delta + u_k^d - x_k^d - P_k^d\Delta - v_k^d\big]^+$（在最优解中不会"多买"来制造缺额）。
  2. 充放电功率与储电量边界同问题 1。
  3. **跨日状态传递**：$E_{144}^{d}=E_{0}^{d+1}$；$E_0^{1}=6000$ kWh。
  4. **每日终端储电量**：题目未规定（见 §7 冲突 C-1），主模型取"终端自由"，并在报告中给出"每日终端=当日初始"的对照。
- **目标**（第 $d$ 天）：
  $$\min\;\; J^d=\underbrace{\sum_{k=1}^{144} p_k x_k^d}_{\text{计划购电费用}}+\underbrace{5\sum_{k=1}^{144} p_k r_k^d}_{\text{紧急购电费用}}$$
- **输出要求**：
  - 论文：按表 1、表 2 格式给出 `2025.3.20`、`2025.6.21`、`2025.9.23`、`2025.12.21` 四个指定日期的结果；按**表 3** 格式给出这四天的紧急购电量（表 3 是 4 个日期并排，每个日期下含 `时间段`/`购电量` 两列）。
  - 文件 `result2.xlsx`：三张表，规格见 §1.6（`充放电量`、`紧急购电量` 必须自行扩到 334 天）。

### 3.3 重难点

1. **"计划"与"实际"的信息结构**（本题最大的语义风险，见 §7 冲突 C-3）。题目把附件2 明确标为"实际功率"，而"计划购电费用按计划购电量计算"这句话只有在**计划量 ≠ 实际用量**时才有意义，这暗示出题方设想了一个"计划—实际"缺口。但问题 2 没有给出任何负载或光伏的预报数据。两种自洽读法：
   - **读法 ①（完全信息，推荐为主模型）**：0:00 制定计划时已知当天负载与光伏（附件2），因此最优计划恰好覆盖负载，$r_k^d\equiv 0$，表 3 四天均为 0。此时紧急购电机制是一个"威胁"——它保证了模型即便在计划不足时也仍有可行解，并解释了为什么"计划不足"永远不该发生（因为 5 倍罚价 ≫ 计划电价）。
   - **读法 ②（不完全信息，作为备选并列交付）**：0:00 只能依据当天可得的**典型日预测**（附件1 的负载/光伏曲线，或前一日实际值等）制定计划，实际为附件2 ⇒ $r_k^d>0$，表 3 出现非零值。此时问题 2 变成一个"计划量与罚价权衡"的两阶段问题：多买要按计划价付钱、少买要按 5 倍价补，最优计划量落在缺额分布的高分位上。
   **构思手建议**：主模型用读法 ①（它是题目文字与数据的唯一直接支持：问题 2 未提供任何预报），但**两套结果都算、都交付**，并把读法 ② 的后果（表 3 的非零值、费用的增量）写在论文的稳健性一节。理由：题目问题 3 才引入预报，问题 2 的紧急购电机制更像为问题 3 做铺垫；但表 3 若全为 0 会显得"没答"，所以必须两条腿走路。**这一条请用户优先拍板。**
2. **报告区间为什么从 2.1 开始**：附件2 覆盖 1.1–12.31，但结果文件只要求 2.1–12.31（334 天）。合理解释是 1 月作为**启动/预热期**（burn-in），让储电量摆脱"人为给定的 6000 kWh"初值。主模型从 1.1 开始仿真、报告 2.1 起，并做"预热期长度"的稳健性检验。
3. **一年 334 个 LP 的工程化**：必须函数化（`lib/` 下一个 `solve_day(...)`），统一符号，避免复制粘贴；每天的输出落盘后再汇总。
4. **储电量的跨日传递与终端条件**：终端自由 ⇒ 每日末储电量会被压到下限 1200 kWh（因为价格逐日相同，晚间保留电能没有额外价值，次日凌晨可以更便宜地重买）。这不是错误，但必须在论文里解释清楚，并给出"每日终端=当日初始"的对照解以证明其代价很小（预计费用差在 1% 量级以内，需实测）。
5. **单位与精度**：模板 `购电量` 列格式为 4 位小数（§1.6），交付统一保留 4 位小数。

### 3.4 得分点

> **得分点**：明确写出"计划购电费用按计划量结算"这一**照付不议**（take-or-pay）性质，并据此推出结论命题：在完全信息下 $r^\*_k\equiv 0$，且"计划量恰等于净负荷"是最优的——这需要一句像样的证明（提示：$p_k>0$、$5p_k>p_k$，任何"计划量 > 净负荷"的解都能同额削减 $x_k$ 而降低成本；任何"计划量 < 净负荷"的解都能把 $r_k$ 换成 $x_k$ 而降低成本）。
> **得分点**：把"紧急购电"写成 $r_k=[\cdot]^+$ 的分段线性形式，指出它使目标函数仍为凸（可化为 LP），而不是引入非线性。
> **得分点**：给出全年 334 天的费用汇总（总费用、日均费用、与不储能基线的对比、全年节省额与节省率），并给出四指定日期的表 1/表 2/表 3。
> **得分点**：解释终端储电量被压到下限的经济含义，并用"每日终端=当日初始"的对照定量说明其影响。
> **易错点**：把紧急购电价格 5 倍加在**全部**购电量上（只加在缺额上）；忘记跨日储电量传递（把每天当独立问题，初始储电量一律取 6000）；只报 334 天中的某一天；`充放电量`/`紧急购电量` 表没扩到 334 天。

### 3.5 候选模型

| 候选 | 内容 | 评价 |
|---|---|---|
| **M1 逐日滚动 LP（推荐）** | 每天一个 LP，状态由前一日传入，终端自由 | 与题面"每天 0:00 制定当天策略"完全对应；规模小、可复算；**主模型** |
| M2 全年联合 LP | 334×144 个时段的单一 LP，首末储电量给定，全程最优 | 费用是 M1 的**下界**（完全预见下界）；变量约 15.8 万，稀疏可解但耗时；**作为上界对照与稳健性证据**，非交付主方案 |
| M3 逐日 LP + 每日终端=当日初始 | 每日闭环 | 排除跨日搬运；用作**终端条件的稳健性对照** |
| M4 两阶段随机规划（读法②） | 计划基于可得预测，实际为附件2，含缺额罚价 | 仅在采用读法② 时启用；作为**备选交付** |

**选型结论：主模型 M1；M2 作为最优性上界的交叉验证；M3 与 M4 作为稳健性/备选。** 若用户裁定采用读法②，则主模型改为 M4、M1 作为其退化对照。

### 3.6 与其它问的耦合

- 储电量跨日传递的代码、目标函数分解（计划/紧急两项）在问题 3、4 中直接**扩展**为三项/四项，不重写。
- 问题 3 的"计划购电量"就是问题 2 的计划购电量再加上预报信息的价值；**问题 3 若不使用任何调整（$y_k\equiv x_k$）且把预报当作精确值，应退化为问题 2**——这是一条必须做的自检。
- 问题 4-2 是问题 2 换上附件4 电价；问题 4-3 是问题 3 换上附件4 电价。**同一套 `lib/` 代码，只换价格输入矩阵**。

### 3.7 待确认事项

C-1（每日终端储电量条件）、C-2（$E_0$ 是否锁定 6000 与预热期）、C-3（紧急购电的信息结构——**最高优先级**）、C-4（效率口径）、C-5（报告区间起点 2.1 的处理）。

---

## 四、问题 3

### 4.1 问的是什么

**题目原文关键句**：
> "在每天 0:00、6:00、12:00 和 18:00 可获得未来 24 小时整点的光伏发电功率预报。可根据 0:00 的预报，制定当天的计划购电策略，也可根据其他时刻的预报，调整购电策略。计划购电量高于调整购电量的部分，违约电价是交易时刻电价的 50%。调整购电量高于计划购电量的部分，超出部分的电价是交易时刻电价的 1.5 倍。总的购电费用包括计划购电费用、紧急购电费用和调整购电量的相关费用。请在上述条件下，建立微网当天购电策略的数学模型。"
> "根据附件 1 中的电价、附件 2 中的小区负载和光伏发电及附件 3 的光伏发电预报数据，在每天 0:00 制定当天的计划购电策略，在其他预报时刻制定调整购电策略。请在论文中按表 1、表 2 和表 3 的格式给出表 3 中指定日期的结果，并将完整的购电策略保存到文件 result3.xlsx 中（模板文件见附件 5）。**请分析是否需要引入其他时刻的预报制定调整购电策略。**"

**用自己的话复述**：光伏出力不再已知，只能通过 0:00/6:00/12:00/18:00 发布的滚动预报获知；负载（附件2 的小区负载）仍视为已知。0:00 用当天的预报定下"计划购电量"，之后在 6:00、12:00、18:00 可以依据新预报把当天**尚未执行时段**的购电量改成"调整购电量"。若调整量低于计划量，低的那部分按 0.5 倍电价结算（违约）；若高于计划量，高的那部分按 1.5 倍电价结算（超用）；若实际供给仍低于负载，缺额按 5 倍电价紧急购电。总费用 = 计划购电费用 + 紧急购电费用 + 调整购电量的相关费用。最后要**定量回答**"有没有必要用 6:00/12:00/18:00 的预报做调整"。

### 4.2 已知 / 未知 / 决策变量 / 约束 / 输出要求

- **已知**：$p_k$（附件1）；$L_k^d$（附件2 的小区负载，视为已知）；$\hat P^{(d,\tau)}_m$（附件3 在日期 $d$、发布时刻 $\tau\in\{0,6,12,18\}$、提前期 $m=1,\dots,24$ 的预报值，kW）；实际光伏 $P_k^d$（附件2，只在对账/评估时使用）；储能参数。
- **决策变量**：计划购电量 $x_k^d$（0:00 决策，全天 144 段）；调整购电量 $y_k^d$（有效调整后该时段的最终**购电量**，覆盖 144 段）；充放电量 $u_k^d,v_k^d$；储电量 $E_k^d$。
  - **调整的生效规则（推荐）**：时段 $k$ 的最终购电量 $y_k$ 由**该时段之前最晚一次调整**决定：
    $y_k=x_k$（$k\le 36$，即 0:00–6:00）；$y_k=y^{(6)}_k$（$6{:}00<k\le 12{:}00$）；$y_k=y^{(12)}_k$（$12{:}00<k\le 18{:}00$）；$y_k=y^{(18)}_k$（$k>18{:}00$）。即"已执行时段不可追溯调整"。
- **约束**：
  1. 每条决策都必须在**当时的预报信息**与储能状态可行性下成立（多阶段/滚动决策的核心约束）。
  2. 供给充足：$y_k^d + P_k^d\Delta + v_k^d + r_k^d \ge L_k^d\Delta + u_k^d$，缺额为 $r_k^d$。
  3. 充放电功率、储电量边界、跨日传递同问题 2。
  4. 非负。
- **目标**：
  $$J^d=\underbrace{\sum_k p_k x_k^d}_{J_{\text{plan}}}+\underbrace{5\sum_k p_k r_k^d}_{J_{\text{emg}}}+\underbrace{\sum_k\Big[\tfrac12 p_k\,(x_k^d-y_k^d)^{+}+\tfrac32 p_k\,(y_k^d-x_k^d)^{+}\Big]}_{J_{\text{adj}}}$$
  （**两种结算口径见 §7 冲突 C-6，推荐上式**；另一种口径把未取用的计划量按 0.5 倍**替代**原价结算，即 $J_{\text{plan}}=\sum_k p_k\min(x_k,y_k)$，总费用会低 $\sum_k p_k(x_k-y_k)^+$。）
- **输出要求**：
  - 论文：按**表 1、表 2、表 3** 三种格式给出 `2025.3.20`、`2025.6.21`、`2025.9.23`、`2025.12.21` 的结果（表 1 给指定时段购电量与全天量/费；表 2 给六个 4 小时段的充放电量与 0:00/24:00 储电量；表 3 给紧急购电）。
  - 必须有一节**定量回答**"是否需要引入 6:00/12:00/18:00 的预报"。
  - 文件 `result3.xlsx`：四张表，规格见 §1.6（`充放电量` 需扩到 2004 行；`调整购电量` 填**调整后的购电量**，与 `计划购电量` 逐格可比）。

### 4.3 重难点

1. **预报 → 144 个 10 分钟值的信息分解**（本题最实、最容易做出差异的一步）。附件3 只给 24 个整点值，计划需要 144 个值。必须给出可辩护的分解规则并做灵敏度：例如用典型日（附件1）的日内形状按小时配比缩放，或用附件2 历史的"整点值→小时块内分布"的平均形状。**分解方式会直接影响问题 3 的结果**，必须写清并做对比实验。
2. **多阶段决策的"不能追溯"约束**：0:00 定的计划对 0:00–6:00 已生效，6:00 的调整不能改过去。若忽略这一点会让调整量虚增、结果失去意义。
3. **预报只覆盖光伏、负载视为已知**：题目给出的预报只有光伏，附件2 的负载直接已知。这个不对称必须在模型假设里写明，不能默默假设负载也有预报。
4. **计划的"锚定"效应**：0:00 的计划一旦定下，任何偏差都要付 0.5/1.5 倍的价差，因此最优计划并不是"对预报的期望最优"，而是**对偏差成本也最优**的锚点——这是本题的数学趣味所在，也是一个得分点。
5. **"是否需要其他时刻的预报"要定量回答**：至少给出三种策略的费用对比：(a) 只用 0:00 预报（无调整，$y\equiv x$）；(b) 用 0:00+6:00；(c) 用 0:00+6:00+12:00+18:00（全调整）。再给 (d) 完全信息（用附件2 实际光伏）作为下界。用费用差与避让的紧急/违约费用说明边际收益，并给出结论与适用边界。
6. **紧急购电在本题才真正发生**：因为预报有误差（§1.4 实测 RMSE 数百 kW），实际供给可能低于负载，$r_k$ 非零，表 3 有实质内容。
7. **`调整购电量` 的含义**：必须与 `计划购电量` 同量纲可比（填"调整后的购电量总量"，不是增量），否则 0.5/1.5 倍的判定无从对照。

### 4.4 得分点

> **得分点**：把问题写成**多阶段（滚动）决策模型**而不是"用实际值做的确定性优化"——明确指出 $\hat P^{(d,\tau)}$ 的信息集 $\mathcal F_\tau$ 逐级增大，每个决策时刻的计划/调整只依赖于当时可得的预报。
> **得分点**：预报的 10 分钟分解规则有依据、可复算、有对比实验（至少两种分解）。
> **得分点**：定量回答"是否需要其他时刻的预报"：给出四种策略的费用表 + 边际收益递减的结论 + 适用边界（例如"当预报精度低于 X 时调整无收益"）。
> **得分点**：紧急购电量落到表 3 的具体时段上，并解释这些时段为何出现缺额（通常是傍晚光伏骤降且储能已放空的时段）。
> **易错点**：用附件2 的实际光伏去制定 0:00 的计划（等于偷看答案）；允许 6:00 的调整修改 0:00–6:00 的已执行时段；`调整购电量` 填成增量；只算费用不算紧急购电；把 0.5/1.5 倍的价差加到全部电量而非偏差部分。

### 4.5 候选模型

| 候选 | 内容 | 评价 |
|---|---|---|
| **M1 多阶段滚动 LP（确定性等价，推荐）** | 以附件3 的预报为情景，按"每个决策时刻只依赖当时预报"的顺序求解 4 个串联 LP（每个 LP 的时段范围依次缩小），实际值只在评估阶段代入 | 与题面完全对应、可复算、可扩展；**主模型** |
| M2 两阶段随机规划 / 鲁棒优化 | 用历史预报误差分布（§1.4 可反演）构造情景集，最小化期望费用或最坏情形费用 | 数学层次更高、创新性强；需构造误差分布并承担计算量；**作为创新加分项并列**，用于验证 M1 的稳健性 |
| M3 完全信息优化（下界） | 直接用附件2 实际光伏，$y\equiv x$、无偏差 | 给出费用下界，用来量化"预报不准的代价"；**必做的对照** |
| M4 仅用 0:00 预报 | 无调整 | 回答最后一问的基准策略 |

**选型结论：M1 为主模型，M3/M4 作为对照（直接服务于最后一问），M2 作为创新点与稳健性验证（时间允许时做，做出期望费用与 CVaR/最坏情形的对比）。**

### 4.6 与其它问的耦合

- 与问题 2 共用：储能模型、逐日 LP 框架、跨日状态传递、目标函数分解。
- 与问题 4-3 的关系：**问题 4-3 = 问题 3 的价格输入换成附件4**，代码必须参数化到"只换一个价格矩阵"。
- 问题 3 的"调整机制"在问题 4-3 中同样存在；不要再写第二套。

### 4.7 待确认事项

C-6（调整购电量的结算口径）、C-7（调整的生效规则与"已执行时段不可追溯"）、C-8（`调整购电量` 填总量还是增量）。**其中 C-8 影响交付文件能否被外部审计，务必确认。**

---

## 五、问题 4

### 5.1 问的是什么

**题目原文关键句**：
> "实际上，外网的电价也是实时波动的。请针对波动电价，建立微网当天购电策略的数学模型。"
> "根据附件 2 中的小区负载和光伏发电、附件 3 中的光伏发电预报和附件 4 中的电价数据，在波动电价下重新计算问题 2 和问题 3。请在论文中给出相应的计算结果，并将完整购电策略分别保存到文件 result4-2.xlsx（对应于问题 2）和 result4-3.xlsx（对应于问题 3）中（模板文件见附件 5）。"

**用自己的话复述**：把"电价每天相同（附件1）"这一假设去掉，改用附件4 的逐日逐时段波动电价，重做问题 2 与问题 3 的全部计算，分别输出到两个文件。**问题 4-2 是问题 2 的价格替换版，问题 4-3 是问题 3 的价格替换版。**

### 5.2 已知 / 未知 / 决策变量 / 约束 / 输出要求

- **已知**：$p_k^d$（附件4，365×144）；$L_k^d$、$P_k^d$（附件2）；$\hat P^{(d,\tau)}_m$（附件3）；储能参数。
- **决策变量/约束**：与问题 2 / 问题 3 完全相同，仅价格输入改变。
- **输出要求**：
  - `result4-2.xlsx`："波动电价下问题 2 的**全部**结果"——按 result2 的三张表结构（`计划购电量` 335×147、`充放电量` 2004 行、`紧急购电量` 334 天）。
  - `result4-3.xlsx`："波动电价下问题 3 的**全部**结果"——按 result3 的四张表结构。
  - 论文：给出计算结果（可沿用表 1/2/3 格式给出四个指定日期，并给出全年汇总与"波动电价 vs 固定电价"的对比）。

### 5.3 重难点

1. **跨天套利是否值得做**（本题唯一的实质性新增难点）。价格逐日不同，理论上可以"低价日多充、高价日多放"。但实测（§1.5）显示：附件4 的日内形状与附件1 完全相同，差异只是一个逐日乘性因子（0.69–1.21）加 ±10% 日内噪声；日内峰谷价差 3.758 倍远大于跨天最大价差 1.75 倍。因此**跨天套利的空间被日内套利挤占**，逐日解耦的近似误差应当很小——但**必须用全年联合 LP 实测验证**，不能只靠这段论证。
2. **逐日终端储电量条件在波动电价下的正当性**：固定电价时"终端自由"是合理的（次日凌晨可以便宜地重买）；波动电价下如果明天电价明显更高，今天留电就有价值。"终端自由"会系统性低估储能价值。**推荐：问题 4 的主模型加入终端储电量的余值项（terminal value），取次日（或未来 24 h）可替代购电的边际成本，使逐日滚动决策与多日最优一致**；这是本题可以拿创新分的地方，并且直接呼应问题 2/3 的同类处理。
3. **工作量翻倍**：问题 4 要跑两套（4-2、4-3），共约 668 个日 LP（4-3 每天还有 4 个决策时刻）。必须先把 `lib/` 写稳，一次跑通，随机种子/求解器设置固定。
4. **表格填法**：result4-2/4-3 的模板只有示例行，仍需自行扩表（同 §1.6）。

### 5.4 得分点

> **得分点**：明确指出并**定量验证**"附件4 电价 = 附件1 典型日形状 × 逐日因子"这一结构（逐列比值均值 1.0000、日因子标准差 0.1401），据此论证逐日滚动策略在波动电价下仍然接近最优，并给出全年联合 LP 的下界作为证据。
> **得分点**：引入终端储电量余值（terminal value）处理逐日滚动与跨日耦合的矛盾，并做"终端自由 / 终端=初始 / 加余值"三者的费用对比。
> **得分点**：四个指定日期在波动电价下的结果与固定电价结果的直接对比表（购电量、购电费、紧急/调整费用、储电量轨迹），并解释差异来源（哪几天的电价因子高、哪些时段价差被放大）。
> **易错点**：直接把问题 2/3 的结果按比例缩放当作问题 4（价格是逐日逐时段不同，不能缩放）；忘记扩表；只做 4-2 不做 4-3 或反之；仍然用"每天电价相同"的结论解释问题 4。

### 5.5 候选模型

| 候选 | 内容 | 评价 |
|---|---|---|
| **M1 逐日滚动 LP + 终端余值（推荐）** | 每天一个 LP，终端储电量按次日的可替代边际购电成本估值 | 与问题 2/3 同构、可执行、避免低估储能价值；**主模型** |
| M2 全年联合 LP（完全预见） | 单一 LP，全程最优 | 除计算量大外无缺点；给出**费用下界**，是 M1 最优性的直接证据；**必做对照** |
| M3 逐日 LP + 终端自由 | 最简近似 | 实现最省事；系统性低估储能价值；**作为稳健性对照** |
| M4 价格随机过程 + 随机规划 | 把 $p$ 也视为随机量做两阶段随机规划 | 创新性最强，但问题 4 要求的是"重新计算问题 2 和问题 3"，属加分项而非必需；时间允许再做 |

**选型结论：M1 为主模型，M2 作为下界对照，M3 作为稳健性对照，M4 视时间作为创新加分项。**

### 5.6 与其它问的耦合

- 问题 4 不应产生任何新符号、新代码路径：**唯一的输入差异是价格矩阵**。若编程手写出了"问题 4 专用"的函数，视为设计不达标。
- 问题 4-2 与问题 2 的结果差 = 波动电价带来的费用变化；问题 4-3 与问题 3 的结果差 = 波动电价 + 预报误差的联合效应。这两组对比是论文很好的分析素材。

### 5.7 待确认事项

C-9（终端余值的取法）、C-3（问题 4-2 是否沿用问题 2 的信息结构读法）。

---

## 六、四问之间的关系与可复用资产

| 复用对象 | 说明 |
|---|---|
| 数据读取层 | 附件1/2/4 都是"日期 × 144 时段"的宽表，附件3 是长表 → 统一读成 `(365,144)` 或 `(365,144,·)` 的数组，放 `lib/` |
| 储能模型 | $E_k=E_{k-1}+\eta u_k-v_k/\eta$、功率与储电量边界，四问完全一致，只写一次 |
| 时间对齐 | §1.2 的时段定义与 §1.4 的预报对齐，只定义一次（`lib/timegrid.py`） |
| 价格来源 | 问题 1/2/3 用附件1 广播到 365 天；问题 4 用附件4。同一个接口 `get_price(source)` |
| 逐日 LP | 一个问题参数化函数 `solve_day(price, load, pv_plan, pv_actual, E0, E_end_mode)`，四问全部由它派生 |
| 结果写出 | 模板填表逻辑（含 `充放电量` 扩表、`紧急购电量` 聚合时段）只写一次，四问共用 |
| 表 1/2/3 汇总 | 由落盘结果反查生成，保证论文数字与文件一致（数字可追溯） |

**符号复用**：$p,L,P,\hat P,x,y,u,v,E,\eta,\Delta,k,d,\tau,m$ 在四问中共用；不新增 $t_{\text{first}}$ 之类冗余符号（见 `符号表.md`）。

---

## 七、题目规定 vs 工作区约定：冲突清单（**已全部裁决，权威记录见 `口径与假设台账.md`**）

> 裁决规则（AGENTS.md §0）：**赛题规定 > 用户直接要求 > 工作区约定**。下表凡属"题目未规定"的，才由工作区约定或本解读填补；凡属"题目与工作区约定冲突"的，一律以题目为准，并在此登记。
> 标记：**S**=上级（题目）与工作区约定的真冲突；**G**=题目空白（题目没规定，需我们决定）；**W**=工作区内部自相矛盾（需修订工作区文件，不是题目问题）。
> **状态说明**：用户已于 2026-09 逐条裁决，本表的"裁决"列即最终口径，并已同步进 `口径与假设台账.md`。

| 编号 | 类型 | 内容 | 最终裁决（用户已确认） | 影响 |
|---|---|---|---|---|
| **C-1** | G | **问题 2/3/4 未规定每日终端储电量的条件** | **终端自由 + 跨日传递**（$E_{144}^{d}=E_0^{d+1}$）；"终端=当日初始"作对照解 | 全局费用与每日储电量轨迹；每日末端被压到 1200 kWh 属经济结论、须解释 |
| **C-2** | G | **问题 1 的 0:00/24:00 储电量是否锁定 6000 kWh** | **锁定 $E_0=E_{144}=6000$ kWh**；端点自由（周期最优 54 149.37 kWh / 32 909.87 元）作对照 | 问题 1 购电费：35 126.9486 元 vs 32 909.8653 元（差 6.31%） |
| **C-3** | G | **问题 2 的"计划"依据什么信息** | **主模型=完全信息**（紧急购电恒 0，附命题证明）；**并列交付**"计划基于典型日/前一日预测"的备选结果（表 3 非零） | 表 3 全为 0 还是有非零值；全年费用 |
| **C-4** | G | **"充放电效率 90%"是单向还是往返** | **单向 $\eta=0.9$（往返 0.81）**；往返 0.9 作对照（31 695.89 元） | 全部四问的电量与费用（差 3.7%；套利门槛 1.235 变 1.111，结论不翻转） |
| **C-5** | G | **报告区间从 2025.2.1 开始，1 月怎么用** | **1 月作预热期**：从 2025-01-01 0:00（$E=6000$ kWh）开始仿真，**严格按模板只填 2.1–12.31 共 334 天**；"预热 0 天"作对照 | 2.1 当天的初始储电量 |
| **C-6** | G | **问题 3 的费用结算口径** | **采用用户提出的口径**：$J=\sum_k\big[p_k\min(x_k,y_k)+\kappa_-p_k(x_k-y_k)^{+}+\kappa_+p_k(y_k-x_k)^{+}\big]+\kappa\sum_k p_kr_k$，等价于"计划购电费用 $\sum px$ + 调整相关费用（欠取按 $0.5p$ 退回、超用按 $1.5p$ 加收）+ 紧急购电费用"，与问题 2 在 $y=x$ 时严格一致；对照口径（欠取按全价 + 50% 违约金）另行报出 | 问题 3 / 4-3 的总费用 |
| **C-7** | G | **调整购电量的生效范围** | **不可追溯**：时段取"该时段之前最晚一次决策"的值 | 问题 3/4-3 的调整量与偏差费用 |
| **C-8** | G | **`调整购电量` 工作表填总量还是增量** | 填**总量** $y_k$（与 `计划购电量` 逐格可比） | 交付文件能否被外部直接对照检查 |
| **C-9** | G | **问题 4 逐日滚动的终端储电量余值怎么取** | 默认取"次一日的可替代边际购电成本"（次日最低价 $/\eta$）；"终端自由""终端=初始"作对照 | 问题 4 的储能利用程度与费用 |
| **C-10** | G | **数值精度：题目完全没有规定** | 统一保留 **4 位小数**（与 result1 模板 `购电量` 列的 `0.0000` 一致）；论文表可四舍五入到 2 位并在表注说明 | 外观一致性 |
| **C-11** | S | **模板需填写，但 `附件/` 只读** | 把模板**复制**到 `问题N/` 下同名文件再填写，`附件/` 保持只读 | 交付路径 |
| **C-12** | S | **结果文件名 vs "中文命名"要求** | 结果文件严格用题目规定的英文名；**图片**用中文自解释命名 | 交付命名 |
| **C-13** | W | `roles/编程手.md` §2 写"本机**无外网**、不能 `pip install`"，与实测矛盾 | **已修订该行**（2026-09）：本机有外网、可以 pip install（走代理），`scikit-learn` 仍禁止安装；同时写入 `口径与假设台账.md` D-17 与 `AGENTS.md` §六 | 编程手能否联网取数、能否装包 |
| **C-14** | W | "同一问统一工具" vs "python ↔ matlab 互验" | 不矛盾：**主链用 python**，MATLAB 仅用于**独立交叉验证** | 交叉验证方式 |
| **C-15** | G | **附件5 模板的时段标签比数据右移一格** | 采用**填法 Y-轮转**（§1.2、台账 D-01）：模板第 $k$ 格装当天第 $k{+}1$ 个时段、第 144 格装当天第 1 个时段；聚合与绘图按真实时间顺序；论文中显式说明 | 四个结果文件的逐格取值（错一格可致 20% 偏差） |
| **C-16** | G | **附件4 有 9 个极端低价点（最低 0.0076 元/kWh）** | **保留**；另做"剔除 <0.05 元/kWh 的点后重算"的对照 | 问题 4 的费用与充放电轨迹 |
| **C-17** | G | **能否向电网售电** | **不能**；余电只能充入储能或弃光 | 目标函数（无售电收益项） |
| **C-18** | G | **罚价倍数是否视为常数** | 视为常数（5 / 0.5 / 1.5），但在灵敏度中扰动 | 灵敏度实验设计 |

**结论**：C-1～C-18 已全部裁决完毕，口径固定在 `口径与假设台账.md`。**其中 C-2、C-3、C-4、C-6、C-15 这五条必须在论文中显式说明**（题目未明确 + 我们的处理 + 备选口径的差异），这既是可审计性要求，也是让评委看到我们读懂题意的机会（`AGENTS.md` §1.8）。

---

## 八、总体技术路线与"国一"着力点

**技术路线**（四问共用一条主链）：

1. **数据层**：`lib/dataio.py` 读附件1/2/3/4 → 统一数组；`lib/timegrid.py` 固定 144 时段与预报对齐规则。
2. **模型层**：`lib/storage.py`（储能状态方程与边界）+ `lib/solve_day.py`（参数化逐日 LP，兼容问题 1/2/3/4 与终端条件模式）。
3. **策略层**：问题 1 单日 LP；问题 2 逐日滚动；问题 3 四时刻滚动调整；问题 4 换价格矩阵 + 终端余值。
4. **验证层**：方法侧互验（LP↔DP、python↔matlab、逐日↔全年联合）、数据侧扰动、参数扰动、多因素网格。
5. **交付层**：`result*.xlsx`（严格模板）+ 每问 `.md`（九个固定章节）+ 中文命名图表 + 可复现脚本与运行说明。

**国一着力点（阅卷最看重的四件事，逐条落实）**：

1. **模型的层次**：问题 3 必须写成**多阶段滚动决策**（信息集逐级增大）而不是"用实际值做的确定性优化"；问题 4 必须处理**跨日耦合与终端余值**。这两处是区分"会做"与"做得深"的分水岭。
2. **量化证据**：每一问的关键结论都要有数字支撑（费用、节省率、预报误差的代价、跨天套利的空间占比等），并且数字能从落盘文件逐条追到代码。
3. **灵敏度与稳健性（AGENTS.md §1.7，强制）**：每问至少做四类实验——关键参数扰动（±5%/±10%/±20%）、数据侧扰动（剔异常点/换预处理/增减样本）、方法侧互验（换算法或换工具）、多因素组合扰动（网格/正交）——并明确写出**结论在什么范围内成立、什么范围失效**。扰动导致结论翻转要如实报告。
4. **格式合规**：结果文件名、工作表名、列名、列顺序、单位、精度严格照模板；`充放电量`/`紧急购电量` 自行扩表到 334 天；表 1/2/3 在论文中与文件数值一致。

**红线自检**：全程不使用神经网络、随机森林、XGBoost/LightGBM、SVM 等黑箱模型；光伏预报误差的处理用统计分布/情景构造，不用机器学习拟合；储能与购电决策全部由数学规划与概率统计方法给出，参数有明确业务含义。
