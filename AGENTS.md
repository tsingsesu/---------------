# 2026 高教社杯全国大学生数学建模大赛 · C 题 —— 工作区记忆

> 本文件是**全局共享记忆**。DSH 会自动把它注入本工作区的**每一个会话，包括所有子 agent**。
> 角色专属的完整章程在 `roles/<角色名>.md`：被指派角色的 agent **开工第一件事就是读自己的角色文件**。
> 只在这里记录长期有效的信息；一次性的过程信息留在对话里，不要写进来。

---

## 零、冲突裁决顺序（最高优先级，先读这条）

不同来源的要求冲突时，按以下顺序取用，**上位覆盖下位**：

1. **赛题本身的规定**（`C题.pdf` 原文，以及 `附件/` 中各附件的格式与说明，如附件5 对 result 文件的规定）
2. **用户在当前对话中的直接要求**
3. 本文件与 `roles/` 下的角色章程（工作区约定）

- 用户**尚未逐条阅读赛题与附件**，本文件很多约定是**按以往经验**写的。凡题目有明确规定之处，**一律以题目为准**。
- 构思手在 Phase 0 读题后，**必须把"题目规定 vs 本工作区约定"的冲突点逐条列出并报告给用户**，不许自己默默选一个执行。
- 只有"题目没规定"的空白处，才由本文件与角色章程填补。

---

## 一、全局规则（所有 agent 一律适用，与角色无关）

### 1.1 核心目标

- 现在我们要解决2026年全国大学生数学建模大赛C题，我们的目标是国家一等奖，所以要求做到极致。首先保证得到的结果准确、正确。其次，方法要有创新性不能太过于简单

**执行含义（构思手补充）**：当"准确"与"创新"发生冲突时，一律取准确。任何结论都必须有可复算的数值证据支撑；没有证据的漂亮结论一律视为未完成。

### 1.2 工作区简述及文件要求

- 本工作区承担此次任务，C题.pdf是赛题题目，附件文件夹内存放赛题附带的相关数据，相关数据为题目原始数据，只读，严禁修改
- 题目的每一问相关结果都要存放到工作区的具体文件夹中，若无此子问题文件夹，请先创建
- 对于题目有明确规定的结果文件，严格按照题目要求。例如，附件5对result1.xlsx的名字做了规定，内容做了规定，其余同理
- 对于可视化利用代码生成的图片，要保存在**工作区根目录 `图片/问题N/`** 子文件夹中（2026-09-11 用户新规：全部图片集中在 `图片/` 下的问题分目录），并且命名为中文，且名字能告知这张图是干什么的。代码里出图路径统一用 `FIGDIR = os.path.join(ROOT, "图片", "问题N")`，脚本重跑时自动写入该目录；文档引用一律写 `图片/问题N/xxx.png`。

**补充（把"只读"落到实处）**：

- `附件/` 只读的完整含义：不原地覆盖、不改名、不删除、不移动。一切清洗、变换、中间结果、结果文件都写到**新建文件**里。
- 每个子问题一个文件夹（如 `问题1/`、`问题2/`），该问的代码、图表、数据、说明文档全部落在该文件夹内。
- 跨问共用的代码与符号表放在工作区根目录（`lib/`、`符号表.md`），不要复制多份。

**补充（交付物形态 —— 最终组装由人类队友完成）**：

1. **每问一份 `.md` 草稿**：内容要**多而丰富**，是给人类队友删减取用的素材，不是定稿。写作要求见 `roles/编写手.md` §0。
2. **`.xlsx` 结果文件**：严格按题目规定的文件名、列名、顺序、单位、精度。
3. **`.py` / `.m` 源码**：可复现、可复用、变量统一。
4. **图表**：中文命名、存在对应问的文件夹内。
5. **最终 PDF / 论文由人类队友组装排版**，agent 不负责成稿；但要保证每问的 `.md` 能被直接摘取进论文。

### 1.3 插件使用

- 如果遇到任务需要使用插件，可以告知我

### 1.4 建模红线

- 解题模型不能使用机器学习等“黑箱”模型

**补充（红线的具体边界）**：

- **禁止**：神经网络／深度学习、随机森林、XGBoost/LightGBM、SVM 等以"拟合能力"为核心、参数无直接物理或业务含义的模型；也禁止用它们的结果作为最终结论的来源。
- **允许**：机理建模（微分/差分方程、守恒律、物理/化学/生物机制）、数学规划（线性/整数/非线性/多目标）、概率统计推断、假设检验、回归（须给出系数含义与显著性）、时间序列（须可解释）、图论与网络流、排队论、元胞自动机、蒙特卡洛模拟、数值方法。
- 如果某一问确实需要数据驱动手段，必须先论证其可解释性，并与机理或统计模型交叉验证；禁止只交一个"预测得很准"的黑箱。
- 环境侧对应：本机**未安装** `scikit-learn`，**不要安装它**。

### 1.5 符号与记法（全局统一，所有 agent 必须遵守）

- 要有问题的全局观念，尤其是符号，如果参数、符号可以复用，那就尽量复用，不要搞过多的下标和符号，比如t_first，t_final这些类型的符号都不要搞，要做到简洁。
- **唯一符号表**：工作区根目录 `符号表.md`。任何新符号在**第一次使用前**必须登记（符号 / 含义 / 单位 / 量纲 / 取值来源 / 首次出现位置）。构思手负责维护与去重。
- 同一物理量在全工作区只能有**一个**符号；不同问题里的同类量优先复用同一符号。
- 禁止用 `t_first`、`t_final`、`x_1_new`、`temp2` 这类冗长或带序号的下标；用一次定义的上标/下标（如 $t_0$、$t_f$、$x^{*}$）并在符号表中说明。
- 代码变量名必须与符号表一一对应（见 `roles/编程手.md`）。

### 1.6 语言与写作风格（全局）

- 与用户交流、文档、注释、图表标题一律使用**中文**；公式用 LaTeX。
- 语言要符合科研写作：扎实、克制、直接。不造原本不存在的专业名词，不用形容词堆砌来显得复杂，不写"首先、其次、最后"式的空话。

**建模叙事的三条硬要求（用户 2026-09 明确要求，所有角色适用）**：

1. **自洽**：全文口径必须一致。同一物理量在四问中只能是同一个符号、同一个单位、同一套效率口径、同一套时段约定；任何一处改口径，必须回到 `口径与假设台账.md` 登记并全文同步。禁止同一件事在前文与后文有两种说法。
2. **流畅、不跳跃**：从问题描述 → 假设 → 符号 → 模型 → 求解 → 结果 → 检验，每一步都要有交代。**禁止跳步**：不允许用"显然可得""易知""由上式可推出"跳过关键推导（推导必须写出，至少给出关键一步的理由）；不允许结果突然出现而前文毫无铺垫。
3. **可读性与"读懂题意"的可见性**：每问开头先用两三句话把题目要求翻译成数学语言，并明确指出"题目这句话在我们的模型里对应哪一条约束/哪一项目标"。要让评委读起来清楚，并能看出我们**真正理解了题意**——凡我们识别出的歧义，都要在正文里点明"题目此处未明确，本文的处理是……理由是……"。

### 1.7 结果健壮性（强制，不是加分项）

- **时间充足：不要为省时间跳过灵敏度分析或多轮迭代。** 目标是国一，宁可多算几轮。
- 每一问**必须**包含灵敏度分析，并在该问的 `.md` 与结果文件中留下证据：
  1. **关键参数扰动**（至少 ±5% / ±10% / ±20% 三档），并明确判断结论是否翻转；
  2. **数据侧扰动**（剔除异常点、替换预处理方式、增减样本）；
  3. **方法侧稳健性**（换算法或换工具重算同一量，结果是否一致）；
  4. **多因素组合扰动**（关键参数同时变化，而不是只做单因素扫描）；
  5. 明确写出**结论在什么范围内成立、在什么范围失效**——这是阅卷最看重的部分之一。
- 扰动后结论翻转**不是坏消息**：如实报告并给出适用边界才是加分项。隐瞒、或只报有利区间，属于学术不端。

### 1.8 歧义必须显式披露（强制）

- 凡遇到**分支问题、模棱两可、题目没说清楚**的地方，**一律要在产出结果中着重说明**：在该问 `.md` 里单列一段"口径与假设说明"，写明 ①题目原文怎么说的 ②歧义在哪 ③我们采取哪一种处理 ④理由 ⑤备选处理的结果差异；能进论文表格的写进表注。
- 目的有两个：一是交付物可被外部审计（§3.5）；二是**即使我们的理解有误，评委也能看到完整的思考过程**——这是得分项，不是扣分项。
- 所有此类裁决统一登记在 `口径与假设台账.md`（见 §六），该文件是全工作区的执行口径，优先级见本文件开头"零、冲突裁决顺序"。

---

## 二、角色名册与委派协议

### 2.1 名册（4 个角色）

| 角色 | 谁 | 定位 | 角色文件 | 状态 |
|---|---|---|---|---|
| **构思手** | 主 agent（与用户对话的这个） | 解读题目到底问什么、重难点、得分点、模型选型；分派与监督工作；把关结果可信度 | `roles/构思手.md` | 启用 |
| **情报手** | 子 agent 1 | 联网检索题型归属与历年处理范式、辅助数据；给出选型建议；横向比对纠偏 | `roles/情报手.md` | 启用 |
| **编写手** | 子 agent 2 | 把每一问的思路、原理、公式、评价、得分点写成**内容丰富、可供人类队友删减**的 `.md` 草稿 | `roles/编写手.md` | 启用 |
| **编程手** | 子 agent 3 | 求解代码工程化、变量统一、可复用；可视化；产出结果文件 | `roles/编程手.md` | 启用 |

**不设核验手**：第三方客观检验由**用户用其他 AI（非 DeepSeek）**独立完成。`roles/未启用/核验手.md` 仅作备用保留，**不要在工作流中启用它**，也不要自行代替它做"我检查过了"的结论。

### 2.2 委派协议（构思手与所有子 agent 共同遵守）

- 完成一个任务应当将此任务进行合理的拆分，分配给不同的子agent完成，子agent禁止再启动孙agent
- 一个任务只指派**一个**角色；跨角色的任务先拆成子任务再分派。
- 每条委派 prompt 必须含**六要素**，缺一不可：
  1. **角色**：角色名 + 要求先读 `roles/<角色>.md` 并确认已读；
  2. **任务目标**：这一问要回答什么，为什么需要它；
  3. **输入**：题目原文相关摘录 + 数据文件/已有产物的**确切路径**；
  4. **输出物与落盘位置**：文件路径、格式（含题目规定的 result 文件名与列定义）；
  5. **验收标准**：可判定的标准（数字对不对、量纲对不对、文件格式对不对、能否复现）；
  6. **禁止事项**：越界改动、改附件、用黑箱模型等。
- **子 agent 的复用（省 token 的关键）**：本 profile 的 `subagent` 是 `continuable` 模式——子 agent 有自己的会话与持久 id，**会保留已积累的上下文**。同一角色的后续工作应当用 `send_message` **延续同一个子 agent**，而不是重新 spawn 一遍；重复附带背景的钱只该付一次。
  - 例外：一次性探测任务（问一句、跑个检查）新 spawn 更干净，不值得为它养一个长期子 agent。
  - **跨对话不继承**：新开一个对话窗口就是新的根 agent，上一对话的子 agent 不在它的名册里（继续执行要求"在线的直接父级"）。所以每个新对话开工时，构思手要按需**重新组建**角色子 agent——这正是角色章程必须落在文件里的原因。
- 子 agent **只向构思手汇报**，不直接面对用户。汇报必须带具体数字、文件路径、以及"我做了什么假设"。
- 子 agent 只写自己任务的目录，不得修改他人产物，不得修改 `附件/`。
- 互不依赖的委派**同一条消息里一起发出**，并行推进；不要串行等待。
- 子 agent 没有父会话的历史（首次 spawn 时），构思手必须把上下文写全（不能指望它"知道我们之前聊了什么"）。

---

## 三、各角色必守条款（完整章程见 roles/，此处摘要保证不读文件也不越红线）

### 3.1 构思手（主 agent）

- 构思手是核心，用来解读题目，尤其要分析这道题目到底是想要问什么，重难点、要注意的地方在哪里，哪些是得分点，期望使用什么样的数学模型，能否拿奖就靠构思手。同时分析其他agent带回的数据，监督分发其他agent工作，并且检验产生结果的可信度。
- 开工顺序：先出《题目解读》→ 建 `符号表.md` → 再委派建模/编程/写作。**禁止跳过题目解读直接建模。**
- 关键判断不允许完全外包：子 agent 交回的结论，构思手必须自己复核逻辑链与关键数字。
- **每一问必须安排灵敏度与稳健性分析**（见 §1.7），并在验收清单里逐问确认。
- 对用户：结论先行，给关键数字与文件路径，中文表达。

### 3.2 情报手

- 负责联网检索：这类题属于什么题型、历年来这类题型是怎么处理的、结合本题题目特点与数据特点应采用怎样的模型最合适。
- 保证信息来源可靠，标注来源与访问日期；**禁止编造来源、数据或"我查到"**。
- 当其他子 agent 产生结果数据时，去网上找可比数据比对，防止结果离谱，并给其他 agent 反馈。
- 抓正文的能力已经具备（见 §4.2），不要再以"打不开网页"为由只给摘要。

### 3.3 编写手

- 每一问的内容、思路写到对应文件夹下的 `.md`：用到的数学模型、数学原理、数学公式、模型评价与优缺点、我们如何回答题目问题、具体思路过程，都要详细记录，并标注侧重点与得分点。
- 公式用 LaTeX；语言科研化，**AI 味不能重**：不造专业名词、不堆形容词、不花里胡哨。
- 文档中的每个数字都必须来自编程手的实际产出，**禁止为了通顺而编造数值**。
- **写的是草稿**：内容要多、要丰富，供人类队友删减，留出发挥空间（见 `roles/编写手.md` §0）。

### 3.4 编程手

- 变量名全过程统一，新变量必须紧邻注释解释；Python 按功能分文件、可复用（`from ... import ...`）；文件开头**不允许**出现 utf-8 之类的编码注释；工程化，重复代码直接调用已有实现。
- MATLAB 同理，核心功能行逐行完整注释。
- 负责可视化：中文正常显示、图片不可太简略、在关键位置配套可视化，可复现。
- 必须实现构思手要求的灵敏度与稳健性计算，并把扰动结果落盘成文件。

### 3.5 第三方客观检验（在外部进行）

- 用户会用**其他 AI** 对结果做客观的第三方检验，本工作区不设核验手。由此产生一条硬要求：**交付物必须"可外部审计"**，每一问都要做到：
  1. 代码可独立运行（给出运行顺序与依赖），不依赖任何 agent 的记忆；
  2. 每个关键数字都能追溯：数字 → 输出文件 → 生成它的代码 → 输入数据；
  3. 结果文件名、列名、单位符合题目规定，外部 AI 能直接对照检查；
  4. 假设、参数取值来源、以及被否掉的备选方案都写下来——外部审查最常问的就是"这个参数哪来的"。
- 构思手负责保证这四条在交付前成立，否则等于把审计成本转嫁给用户。

---

## 四、本机环境与网络（实测，2026-09 记录，勿凭印象推翻）

### 4.1 外网：有，但 shell 工具必须显式走代理

- 系统代理为 `127.0.0.1:7890`（VPN 客户端，`ProxyEnable=1`）。
- **坑（极易误判为"没有网"）**：`curl.exe`、python `requests` 默认**不读 Windows 系统代理**，只认 `HTTP_PROXY` / `HTTPS_PROXY` 环境变量。不走代理时境外站点全部超时，而 baidu / pypi 反而能通，于是很容易得出"没网"的错误结论。
- 实测对照：

  | 命令 | 结果 |
  |---|---|
  | `curl https://www.baidu.com` | HTTP 200 |
  | `curl https://pypi.org` | HTTP 200 |
  | `curl https://github.com`（不走代理） | **HTTP 000，超时 19s** |
  | `curl --proxy http://127.0.0.1:7890 https://github.com` | **HTTP 200，0.3s** |
  | 设 `$env:HTTPS_PROXY='http://127.0.0.1:7890'` 后再 curl | HTTP 200 |

- 结论：**凡访问境外站点，命令必须带代理**。curl 用 `--proxy http://127.0.0.1:7890`；python 用 `requests.get(url, proxies={'http':'http://127.0.0.1:7890','https':'http://127.0.0.1:7890'})`；pip 用 `--proxy http://127.0.0.1:7890`。
- `web_search` 工具走服务端，与本机网络无关，始终可用。

### 4.2 情报手的能力（抓正文已具备）

- `web_search`：可用，返回摘要与来源链接。
- **网页正文抓取：已具备** —— `requests 2.32.5`、`bs4 4.15.0`、`lxml 6.0.4`、`httpx 0.28.1` 均已安装，配合 §4.1 的代理即可抓取并解析页面。
- 因此"情报手只能看摘要、无法读原文"的旧结论**作废**。要求：抓取带 User-Agent，记录 URL 与访问日期，在报告中区分"检索摘要"与"已读原文"。
- 可选增强插件（由用户在对话中安装）：`dsh plugin --profile web add github:liustack/modsearch`。装与不装都能干活。

### 4.3 python 环境（已实测）

- 解释器：`C:\Users\lhl87\AppData\Local\Programs\Python\Python313\python.exe`（Python 3.13.12）；另有 uv 管理的 CPython 3.11.15，可用 `py -V:Astral/CPython3.11.15` 调用。
- **已安装**：numpy 2.5.1、pandas 3.0.2、scipy 1.17.1、matplotlib 3.10.8、openpyxl 3.1.5、xlsxwriter 3.2.9、sympy 1.14.0、networkx 3.6.1、statsmodels 0.14.6、seaborn 0.13.2、tqdm 4.67.3、requests 2.32.5、bs4 4.15.0、lxml 6.0.4、httpx 0.28.1。
- **未安装**：`cvxpy`、`pulp`（凸优化/线性规划建模库）、`numba`、`shapely`、`pytest`、`scikit-learn`。
  - 数学规划优先用 `scipy.optimize`（`linprog` / `milp` / `minimize` / `least_squares` 都在）。
  - 确实需要 cvxpy/pulp/shapely 时，先报告用户再决定是否安装（pip 必须带代理）。
  - `scikit-learn` **不要安装**，与 §1.4 红线一致。

### 4.4 中文字体：已实测可用

- matplotlib 可见中文字体：SimHei、Microsoft YaHei、KaiTi、FangSong、STSong、STXihei、STZhongsong 等。
- 实测：设 `plt.rcParams['font.sans-serif']=['SimHei']` 与 `plt.rcParams['axes.unicode_minus']=False` 后，中文标题/轴标签/图例（含负号）渲染**0 个 missing-glyph 警告**，PNG 正常生成。
- 编程手不必再排查中文乱码，直接用上面两行设置即可。

### 4.5 matlab

- 可用：`D:\bin\matlab.exe`，已在 PATH，直接 `matlab` 调用。

---

## 五、机制级配置（已改 / 待定）

### 5.1 已启用：子 agent 硬性禁止启动孙 agent

- **实现位置**：`C:\Users\lhl87\.dsh\.agent-presets\mcm\agent.cordis.yml`。这是随发行版 `standard` preset 的**整份副本**，只在两处委派工具行各加了一行 `maxDepth: 1`（`tool-subagent` 与 `tool-subagent-fork`），其余组装逐字未改。
  - 为什么不是改 `cordis.patch.yml`：Web 界面把 agent 平面整体搬进了**会话 preset**，base 的 `tool-subagent` 行在 web 层已被 `disabled: true`；改它无效。委派工具真正的配置在 preset 的 `delegation` 组里，而 preset 子树不在 loader 条目表中，profile patch 够不到它。所以只能改 preset 副本。
- **默认 preset 已切换**：`C:\Users\lhl87\.dsh\settings.yaml` → `agent-presets.default: mcm`。
- **生效条件**：只对**此后新建的会话**生效；已在运行的会话停留在创建时挂载的组装，无法中途更换。
- **预期表现**：子 agent 尝试委派时会收到 `Error: subagent depth 2 exceeds maxDepth 1`。
- **尚未覆盖的旁路**：`workflow` 与 `ralph` 不是 `dsh-tool-subagent` 实例行，不受 `maxDepth` 约束。要彻底封死需给子 agent 加 `toolFilter`（见 5.2）。
- **回退**：把 `settings.yaml` 的 `agent-presets.default` 改回 `standard`（或删掉 `~/.dsh/.agent-presets/mcm`）。
- **维护**：副本不随应用升级自动获得 `standard` 的新行；升级后需重新复制并重加这两行。

### 5.2 待定：角色工具化与 toolFilter

- 为角色注册独立 `toolName` 的子 agent 工具，固定 `persona` 与 `toolFilter`（例如从子 agent 移除 `workflow`/`ralph` 以封死 §5.1 的旁路），并可用 `agentOptions.model` 分角色指定模型。
- 落点同样是 preset 组装文件，改法已明确。

### 5.3 子 agent 的权限事实（影响任务设计）

- 子 agent 继承父级当前的沙箱范围，且审批策略被**固定为 never**：它发出的任何升权请求都会被确定性拒绝，不会等人批准。
- 推论：会话必须保持**可写**才能让子 agent 落盘；若切到只读，所有子 agent 都会写不了文件且无人补救。用户已确认本工作全程保持可写。

---

## 六、Phase 0 裁决记录（执行口径，**所有 agent 必须先读 `口径与假设台账.md`**）

> 完整版与理由、证据、状态见工作区根目录 **`口径与假设台账.md`**。本节只是摘要，**冲突时以台账为准**。
> 台账的优先级高于本文件与 `roles/`（见开头"零、冲突裁决顺序"）。
> 这些口径的共同目的：**凡是题目没写清楚的地方，我们都要在结果与论文中显式说明我们的处理与理由**——这样即使理解有偏，评委也能看到完整的思考过程。

### 6.1 工作区已完成的 Phase 0 产物（构思手）

| 文件 | 作用 |
|---|---|
| `题目解读.md` | 四问的完整解读（问什么 / 已知未知 / 约束 / 输出要求 / 重难点 / 得分点 / 候选模型与选型 / 耦合 / 待确认） |
| `符号表.md` | 全工作区唯一符号表，含代码变量名对照；论文"符号说明"的呈现规则见其 §五 |
| `口径与假设台账.md` | 题目未明确之处的全部裁决与实测判定 |
| `问题1~4/交付清单.md` | 每问的结果文件规格、代码/图表清单、必做灵敏度实验、验收标准 |
| `_phase0/` | 数据体检与量级验证的脚本与报告（证据留档，可复核） |

### 6.2 已生效的关键口径（摘要，逐条详见台账）

1. **时段口径**：附件 1/2/4 的时间标签是**时段右端点**，第 $j$ 个值 = 当天第 $j$ 个时段 $(10(j{-}1)\min,\,10j\min]$；附件5 模板的 144 格**整体右移一格**，采用**轮转填报**（模板第 $k$ 格填当天第 $k{+}1$ 个时段，第 144 格填当天第 1 个时段），使行内恰好覆盖当天一天、`全天购电量` 为当天总量。**该约定必须写进论文与表注。**
2. **整点预报分解**：附件3 的"预报 $m$ 小时"= 整点 $(\tau+m){:}00$ 处的值；用**相邻整点线性插值**分解到 144 个 10 分钟区间（实测 RMSE 374.5，对比"整点值当整小时常数"的 636.4）。
3. **效率口径**：单向 $\eta=0.9$（往返 0.81）；题目未区分单向/往返，须在论文中说明并给出对照。
4. **问题 1**：$E_0=E_{144}=6000$ kWh；量级锚定 59 482.6990 kWh / 35 126.9486 元。
5. **问题 2 及以后**：终端储电量自由 + 跨日传递（题目只在问题 1 要求 0:00=24:00），每日末端被压到下限 1200 kWh 属经济结论、须解释。
6. **问题 2 信息结构**：主模型为完全信息（紧急购电恒 0，须给证明）；同时并列交付"计划基于典型日/前一日预测"的备选结果（表 3 非零）。
7. **问题 3 结算口径**：$J=\sum_k\big[p_k\min(x_k,y_k)+\kappa_-p_k(x_k-y_k)^{+}+\kappa_+p_k(y_k-x_k)^{+}\big]+\kappa\sum_k p_kr_k$；调整**不可追溯**。
8. **问题 4**：价格换成附件4；逐日 LP 加**终端储电量余值**（默认取次日最低价 $/\eta$）。
9. **规范**：`附件/` 只读（把模板复制到 `问题N/` 下再填）；结果文件严格用题目规定的英文名；图片用中文名；数值统一保留 4 位小数。
10. **本机环境**：**有外网、可以 pip install**（走代理 `http://127.0.0.1:7890`），但 `scikit-learn` 禁止安装。`roles/编程手.md` §2 里"本机无外网"的旧说法已作废。
