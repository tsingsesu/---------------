# 口径与假设台账（Phase 0 裁决记录，全工作区最高优先级的执行口径）

> **本文件是"题目没写清楚的地方我们怎么定"的唯一权威记录。** 所有 agent（构思手、情报手、编写手、编程手）开工前必须读本文件；凡本文件已裁决的，不得自行另选一套。
> 记录规则：
> 1. 每条记录含 **编号 / 问题 / 裁决 / 依据 / 影响面 / 状态**；状态取值 `已裁决`（用户已拍板或构思手按默认值执行）、`待裁决`（等用户）、`已实测`（由数据判定，不需要人拍板）。
> 2. **凡是与题目理解有关的裁决，必须在最终产出（结果文件说明、该问 `.md`、论文表格表注）中显式写出**——让评委看到我们识别并处理了这个歧义，这是加分项，不是负担（用户 2026-09 明确要求）。
> 3. 新出现的歧义，由构思手登记后再执行；不得"先做了再说"。
> 4. 本文件的裁决**优先于**各问 `.md` 与代码注释中的表述；若发现不一致，以本文件为准并上报构思手。

---

## 一、已裁决 / 已实测项

### D-01 模板时段填法：采用"轮转填报"（用户团队方案，构思手细化）

- **问题**：附件 1–4 的时间标签是 `0:10, 0:20, …, 23:50, 0:00+1`（144 个），附录2 明确 `0:00+1` = 当天 24:00，故这些标签是**时段的右端点**；而附件5 模板的 144 个时段标签却从 `0:10-0:20` 排到 `0:00+1-0:10+1`（result2/3/4 写作 `0:00-0:10+1`），**整体右移了一格**：模板里没有 `0:00-0:10`，却多出 `24:00-24:10`。
- **裁决**：
  1. 数据侧：附件 1/2/4 的第 $j$ 个值 = 当天第 $j$ 个时段 $(10(j{-}1)\min,\,10j\min]$ 的量（$j=1,\dots,144$），144 个时段恰好铺满 $[0{:}00,24{:}00]$。
  2. 模板侧（**轮转填报**）：模板第 $k$ 格（$k=1,\dots,143$，标签为 $(10k\min,10(k{+}1)\min)$）填**当天第 $k{+}1$ 个时段**；模板**第 144 格**（标签 `0:00+1-0:10+1`）填**当天第 1 个时段**（0:00–0:10）。
  3. 于是模板行内 144 格**恰好覆盖当天 $[0{:}00,24{:}00]$ 一次且仅一次**，`全天购电量`/`全天购电费` 就是当天总量；表 1 的 `H:00-H:10` 与模板同标签格子在物理含义上完全一致。
  4. **第 144 格在时间上是最早的一段**：一切聚合（表 2 的六段、图表曲线）必须按真实时间顺序 $j=1,\dots,144$，**不得按模板列序**；绘图时须把第 144 格接回序列开头。
- **依据**：(a) 数据/模板错位是实测事实（`_phase0/报告05~10`、`题目解读.md` §1.2）；(b) 模板第 144 格若按字面取"次日 0:00–0:10"，则 2025-12-31 那一行无数据可用（2026 年数据不存在），必须回退——回退到"当天第 1 个时段"最自然；(c) 轮转填报使每一行自洽（行内=一天），与表 2 逐日填报 `0:00`/`24:00` 储电量的口径一致。
- **影响面**：result1/2/3/4-2/4-3 全部时段格；表 1 的六个指定时段取值（数值见 D-02）。
- **状态**：已裁决（用户团队确认 + 构思手细化）。**必须在每问 `.md` 与论文表注中显式说明。**

### D-02 问题 1 的表格数值（按 D-01 的轮转填法，已实跑 LP）

主模型（$E_0=E_{144}=6000$ kWh、$\eta=0.9$ 单向、填法 D-01）实测：全天购电量 **59 482.6990 kWh**，全天购电费 **35 126.9486 元**，充电量合计 20 740.67 kWh，放电量合计 16 799.94 kWh（比值 0.8100 $=\eta^2$），储电量轨迹触及 1200 与 10800，144 个时段中 58 个购电量为 0，同时充放时段 0 个。表 1 六个时段购电量：

| 时段 | 购电量 (kWh) | 对应数据列（0 基） |
|---|---|---|
| 10:00-10:10 | 0.0000 | 60 |
| 12:00-12:10 | 480.4124 | 72 |
| 14:00-14:10 | 0.0000 | 84 |
| 16:00-16:10 | 445.4317 | 96 |
| 18:00-18:10 | 531.8940 | 108 |
| 20:00-20:10 | 0.0000 | 120 |

- 依据：`_phase0/probe06_q1_scale.py`、`_phase0/报告11_问题1量级体检.txt`。
- 状态：已实测（用于锚定验收区间；正式交付须由编程手独立复算，允许差异仅来自求解器容差与口径）。

### D-03 问题 1 的端点储电量

- **裁决**：问题 1 取 $E_0=E_{144}=6000$ kWh（附录1 给定的 2025-01-01 0:00 储电量），并满足题目"0:00 与 24:00 储电量相同"。
- **对照（必须报出）**：端点自由（周期稳态最优）解为 54 149.3657 kWh / 32 909.87 元，比分主模型再省 6.31%，其 24:00 储电量落在下限 1200 kWh。
- **状态**：已裁决（用户确认）。

### D-04 问题 2 及以后的终端储电量

- **裁决**：题目只在问题 1 提出"0:00 与 24:00 储电量相同"，**问题 2/3/4 不再有此要求** ⇒ 采用**终端自由 + 跨日传递**：$E_{144}^{d}=E_{0}^{d+1}$，$E_0^{1}=6000$ kWh。
- **必须说明的后果**：由于电价逐日相同、且次日凌晨可更便宜地重购，逐日最优解会把每日 24:00 储电量压到下限 1200 kWh（仅在问题 2/3 的价格结构下）。**这是经济结论，不是模型错误**，论文中必须解释；并把"终端 = 当日初始"作为对照解报出（差异预计 1% 量级，以实测算准）。
- **问题 4 例外**：电价逐日波动时"终端自由"会系统性低估储能价值，故事先按默认值采用**终端余值**（见 D-10）。
- **状态**：已裁决（用户确认）。

### D-05 储能效率口径

- **裁决**：**单向效率 $\eta=0.9$**（充电 $E$ 增 $\eta u$，放电 $E$ 减 $v/\eta$，往返 $0.81$）。状态递推 $E_k=E_{k-1}+\eta u_k-v_k/\eta$。
- **对照（必须报出）**：往返 0.9（单向 $\eta=\sqrt{0.9}=0.9487$）口径下，**端点锁定**（与主口径同端点条件）费用为 **33 801.4955 元**（35 126.9486 之上省 1 325.4530 元，差 3.77%）；同一往返口径下**端点自由**费用为 **31 695.8931 元**（D-03 的对照口径）。数值来源：`问题1/主模型运行日志.txt`、`_phase0/报告16_适用边界测算.txt`。套利门槛由 $1/\eta^2=1.235$ 降为 1.111，但典型日峰谷价比 3.758 在两种口径下都远超门槛，**结论不翻转**。
- **必须在论文中显式说明**"题目未区分单向/往返，本文取单向 0.9，并给出往返 0.9 的对照"。
- **状态**：已裁决（用户确认）。

### D-06 问题 2 的"计划"信息结构

- **裁决**（用户确认按构思手建议）：**主模型取完全信息**——0:00 制定计划时已知当天负载与光伏（附件2），最优计划恰好覆盖负载，**紧急购电量恒为 0**；论文中用一条命题给出证明（任一侧偏离都可用同额替换严格降费）。**表 3 四个指定日期均填 0**，并明确写出"完全信息下紧急购电机制不被触发；该机制的作用在问题 3 的不完美预报下才体现"。
- **并列交付（备选读法，必须同时产出）**：0:00 只能依据**当天可得的典型日预测**（附件1 的负载/光伏曲线，或前一日实际值）制定计划、实际为附件2 ⇒ 紧急购电非零，表 3 有实质内容。两套结果都要落盘，论文把备选读法放进稳健性/讨论一节。
- **依据**：问题 2 未提供任何负载或预报数据，完全信息是唯一被题目文字直接支持的读法；但"购电费用按计划购电量计算"暗示的计划—实际缺口又要求我们保留备选读法。
- **影响面**：问题 2、问题 4-2 的表 3 与全年费用。
- **状态**：已裁决（用户确认）。

### D-07 整点预报 → 144 个 10 分钟区间的分解方法（实测判定）

- **问题**：附件3 每条预报只给 24 个**整点**值（题面原文即"未来 24 小时整点的光伏发电功率预报"），而计划与结果文件都要求 10 分钟粒度（144 段）。是分解到 10 分钟，还是直接用整点值当整小时常数，还是干脆按小时粒度建模？
- **实测结论（见 `_phase0/probe07_disaggregate.py`、`报告12_整点预报分解判定.txt`）**：
  1. 附件3 的"预报 $m$ 小时"对应**附件2 中标签为 $(发布时刻+m){:}00$ 的那一列**（即该整点前 10 分钟区间的平均功率）。四种对齐读法的全样 RMSE：**读法 C（整点采样点）370.6** ≪ 读法 A（该小时块均值）563.7 < 读法 B 679.3 < 读法 D 1044.7。50.47% 的格点与实际值完全相等，其余为乘性扰动（比值中位 0.9983、四分位 0.9388–1.0515）。
  2. 附件2 光伏的**小时内形状平均接近线性**（对 $s=6$ 的结点，归一化位置均值 0.1646/0.3342/0.5019/0.6692/0.8373，线性基准 0.1667/0.3333/0.5000/0.6667/0.8333）。
  3. 用 24 个整点预报构造 144 段估计的误差（365 天、0:00 预报）：

     | 方法 | MAE(白天) | RMSE(白天) | RMSE(全样本) | RMSE(非整点列) |
     |---|---|---|---|---|
     | M1 整点值覆盖整小时（分段常数） | 723.7 | 906.0 | 636.4 | 677.1 |
     | **M2 相邻整点线性插值** | **387.3** | **538.4** | **374.5** | **375.3** |
     | M3 整点值 × 历史小时内形状 | 727.2 | 910.4 | 639.4 | 680.5 |
     | M4 整点值 × 典型日小时内形状 | 727.0 | 909.4 | 638.7 | 679.8 |

     纯整点预报误差为 MAE 181.3 / RMSE 370.6；**M2 的全样本 RMSE 374.5 仅比它高 1%**，即线性插值引入的额外误差可忽略；M1/M3/M4 则把误差抬高了 70% 以上。
- **裁决**：**统一采用 M2**——把 24 个整点预报作为结点，**线性插值**得到 144 个 10 分钟区间值。具体地，以整点 $0{:}00,1{:}00,\dots,24{:}00$ 为结点（当天 0:00 这一结点取自**前一日的 0:00 预报的第 24 个值**；2025-01-01 无前一日，该时段为夜间、取 0），在相邻结点间线性插值；整点所在的那一段直接取预报值。
- **对照（必须做）**：把 M1（整点常数）与"小时粒度 LP"作为灵敏度对照，量化粗化带来的费用差。
- **说明**：M3/M4 的表现差是因为"历史小时内形状"按全时段汇总后近似平坦（$S_3\approx[1.0185,1.0103,1.0036,1.0009,1.0002,1]$），无法刻画局部爬坡；若要进一步改进，可尝试**按时刻或按爬坡方向条件化**的形状，属可选的深挖方向，但收益有限。
- **状态**：已实测（数据判定，不需要人拍板）。

### D-08 附件1 的性质

- **实测**：附件1 的三列就是全年 365 天**逐列（逐时刻）均值**——电价最大偏差 $5.2\times10^{-5}$、负载 $5.0\times10^{-5}$（均在舍入量级），光伏 $4.3\times10^{-2}$ kW（相对白天出力可忽略）；三列的均值与全年均值逐位一致。
- **用法**：附件1 = **典型日**。问题 1 使用其三列（电价、负载、光伏预测）；问题 2、3 只使用其**电价列**（广播到 365 天，因题设"每天电价相同"）；问题 4 改用附件4 电价。
- **状态**：已实测。

### D-09 附件4 电价与附件2 负载的结构（问题 4 的依据）

- **实测**：逐点比值 $p^{\text{附件4}}/p^{\text{附件1}}$ 的**逐列均值恒为 1.0000**（范围 0.9999–1.0001）；逐日乘性因子均值 1.0000、标准差 0.1401、范围 0.6876–1.2132；取对数后标准差 0.20197；乘性日因子模型相对残差均值 9.6%。附件2 负载的逐列比值均值同样恒为 1.0000。
- **推论**：日内形状每天相同，跨天差异只是一个 ±20% 量级的日因子 + 约 ±10% 日内噪声。日内峰谷价差 3.758 倍，跨天最大约 1.75 倍，再乘往返效率门槛 $1/\eta^2=1.235$ ⇒ **跨天套利空间远小于日内套利空间**，这是问题 4 沿用"逐日滚动"的定量依据。
- **必做验证**：用**全年联合 LP** 实测逐日滚动策略相对完全预见下界的差距，不得只给论证。

### D-10 问题 4 的终端余值

- **裁决（默认值）**：问题 4 的逐日 LP 在目标中加入终端储电量余值项 $V_E\cdot E_{144}$，$V_E$ 取**次一日可替代购电的边际成本**（即次日最低的"每单位入储成本"$p_{\min}/\eta$）。
- **对照**：终端自由、终端 = 当日初始，两种都要报。
- **状态**：已裁决（按默认值执行）。

### D-11 问题 1 的每日政策与问题 2 的起点

- **裁决**：问题 1 为单日问题，不涉及跨日。问题 2/3/4 的仿真从 2025-01-01 0:00（$E=6000$ kWh）开始，**1 月作为预热期**，结果文件严格按模板只填 **2025-02-01 至 12-31 共 334 天**（"没有 1 月可能是出题人故意的"）。
- **对照**：预热期长度 0 天（直接从 2.1 起、$E=6000$）与 31 天对比，写入稳健性实验。
- **状态**：已裁决（用户确认）。

### D-12 问题 3 的费用结算口径

- **裁决**（用户提出，构思手复核后同意）：设 $x$ 为计划购电量、$y$ 为该时段最终生效的调整购电量、$r$ 为紧急购电量，则

$$J=\sum_k\Big[\underbrace{p_k\min(x_k,y_k)}_{\text{按实际取用计价}}+\underbrace{\kappa_-p_k\,(x_k-y_k)^{+}}_{\text{违约部分按 }0.5p}+\underbrace{\kappa_+p_k\,(y_k-x_k)^{+}}_{\text{超用部分按 }1.5p}\Big]+\kappa\sum_k p_k r_k,\quad \kappa_-=0.5,\ \kappa_+=1.5,\ \kappa=5$$

  等价的分项写法（用于论文的"总费用 = 计划购电费用 + 紧急购电费用 + 调整购电量的相关费用"）：

$$J=\underbrace{\sum_k p_kx_k}_{\text{计划购电费用}}+\underbrace{\sum_k\big[-\kappa_-p_k(x_k-y_k)^{+}+\kappa_+p_k(y_k-x_k)^{+}\big]}_{\text{调整购电量的相关费用（欠取为负、超用为正）}}+\underbrace{\kappa\sum_k p_kr_k}_{\text{紧急购电费用}}$$

- **复核意见（构思手）**：同意用户口径，理由三条：(a) 题面"**违约电价**是交易时刻电价的 **50%**"是对**该部分电量**指定价格，而不是在原价之上再加罚；用户口径对欠取部分只收 $0.5p(x-y)$，与"指定价格"一致。(b) 题面"总费用**包括**计划购电费用…和调整购电量的相关费用"要求分项可加，按上式把欠取的 $0.5p$ 退回写成"调整相关费用为负"即完全吻合，不需要额外发明"退还"科目。(c) 当 $y=x$（不做调整）时 $J=\sum p x$，与问题 2 的"购电费用均按计划购电量计算"**严格一致**，两问口径无缝衔接。
- **必须报出的对照口径**：把欠取部分按"计划量全价照付 + 50% 违约金"处理（即 $J=\sum px+\sum[0.5p(x-y)^++1.5p(y-x)^+]+5\sum pr$）。该口径下欠取部分的实际单价为 $1.5p$，总费用更高；两者差异必须量化。
- **状态**：已裁决（用户提出 + 构思手复核同意）。

### D-13 问题 3 的调整生效规则

- **裁决**：**不可追溯**。时段 $k$ 的最终购电量 $y_k$ 取"该时段之前最晚一次决策"的值：$k\in(0{:}00,6{:}00]$ 用计划量；$k\in(6{:}00,12{:}00]$ 用 6:00 决策值；$k\in(12{:}00,18{:}00]$ 用 12:00 决策值；$k\in(18{:}00,24{:}00]$ 用 18:00 决策值。
- **状态**：已裁决（默认值）。

### D-14 `调整购电量` 工作表的填法

- **裁决**：填**该时段最终生效的购电量总量** $y_k$（不是增量），使该表与 `计划购电量` 逐格可比——题目正是用"计划购电量高于调整购电量的部分 / 调整购电量高于计划购电量的部分"作比较。
- 未被任何调整覆盖的时段（0:00–6:00）填与计划量相同的值。
- **状态**：已裁决（默认值）。

### D-15 数值精度

- **裁决**：题目**完全没有规定**结果精度。统一保留 **4 位小数**（与 result1 模板 `购电量` 列的数字格式 `0.0000` 一致）；论文表格可四舍五入到 2 位并在表注说明。电量 kWh、费用 元、电价 元/kWh、功率 kW。
- **状态**：已裁决（默认值）。

### D-16 其他一并按默认值执行的条目

| 编号 | 内容 | 裁决 |
|---|---|---|
| D-16.1 | 能否向电网售电 | **不能**。余电只能充入储能或弃光（$g_k$ 为弃光量）；目标函数无售电收益项 |
| D-16.2 | 能否从电网购电给储能充电 | **可以**（题面开篇即"电价较低…为储能设备充电"） |
| D-16.3 | 附件4 的 9 个极端低价点（最低 0.0076 元/kWh，集中于 2025-04-26、05-30、09-13、10-10） | **保留**（题目未授权剔除）；另做"剔除后重算"的数据侧扰动对照 |
| D-16.4 | 附件2 光伏夜间的非零小值（<10 kW 共 2678 个）与白天零值 | **不清洗、不插补**；另做"<10 kW 置零"的对照 |
| D-16.5 | 罚价倍数 5 / 0.5 / 1.5 | 视为常数，但在灵敏度中扰动（含 5→4、5→6 等） |
| D-16.6 | 结果文件与附件的关系 | `附件/` 全程只读；把模板**复制**到 `问题N/` 下同名文件再填，不新增到 `附件/` |
| D-16.7 | 结果文件命名 | 严格用题目规定的英文名 `result1.xlsx`、`result2.xlsx`、`result3.xlsx`、`result4-2.xlsx`、`result4-3.xlsx`；**图片**用中文自解释命名 |
| D-16.8 | 结果模板的示例行 | `充放电量`（19 行示例）、`紧急购电量`（11 行示例）**必须自行扩到 334 天**（2004 行 / 334 天）；`计划购电量` 已是 335×147 |
| D-16.9 | `紧急购电量` 填法 | 严格按表 4 示例：一行一天，多个时段用空格分隔、多个电量用空格分隔且与时序一一对应；无紧急购电填 0 |
| D-16.10 | 模板中的笔误 | result2/3/4 第 42 个时段列标签写作 `7:0-7:10`，**原样照抄不改** |

### D-17 本机可联网、可安装包（纠正 `roles/编程手.md` §2 的过时表述）

- **事实**：本机**有外网**；shell 访问境外站点必须显式走代理 `http://127.0.0.1:7890`（pip 用 `--proxy http://127.0.0.1:7890`）。
- **可以 pip install**；但 `scikit-learn` 仍**禁止安装**（`AGENTS.md` §1.4 建模红线）；确需 `cvxpy`/`pulp`/`shapely` 之类时先报告构思手/用户再装。
- **状态**：已裁决。委派编程手时必须在 prompt 中把本条写清，避免它误以为不能装包。

### D-18 论文符号说明的呈现方式（用户提出，构思手同意）

- **裁决**：论文"符号说明"表**只列主符号**（如 $J$ 总购电费用、$p$ 电价、$x$ 计划购电量…），不把 $J_{\rm plan}$、$J_{\rm adj}$、$J_{\rm emg}$ 这类**分项记号**塞进主表。
- **硬约束（防止被判"符号未定义"）**：每个分项记号**必须在公式中首次出现处就地定义**，例如写"其中 $J_{\rm plan}=\sum_k p_kx_k$ 为计划购电费用"；下标的含义必须由字面自解释。
- 主表与分项记号**都必须与工作区根目录 `符号表.md` 一致**；代码变量名仍按 `符号表.md` §五 的对照表执行。
- **状态**：已裁决（用户提出）。

---

## 二、待裁决项（遇到时再谈，先按默认值推进）

用户已明确："这些空白太多了，真正开始做题遇到时再挨个挨个说"。因此下列条目**先按默认值执行**，产出时在文档中标注"此处为本文假设"，遇到具体分歧再回到本台账更新：

| 编号 | 内容 | 当前默认值 |
|---|---|---|
| C-4-细分 | 单向效率是否随充放电方向不同（如充 0.95 / 放 0.85） | 对称 0.9/0.9；灵敏度中做非对称扰动 |
| C-7-边界 | 6:00/12:00/18:00 的调整时刻，是否允许对"正在进行中"的那一个 10 分钟段生效 | 只对整点**之后**的时段生效（保守口径） |
| C-9-细分 | 问题 4 终端余值 $V_E$ 是否按"次日最低价/η"还是"未来 24h 均价/η" | 次日最低价 $/\eta$；两者对照 |
| C-13-补 | 问题 3 负载是否也视为"预报可得" | 视为**已知**（题目只给光伏预报、附件2 直接给负载） |
| C-19 | 附件3 的 18:00 预报覆盖到次日 6:00，而任务只算到当日 24:00；越界部分如何处理 | 只取落在当日 24:00 之前的时段；12-31 的 18:00 预报越界部分忽略 |
| C-20 | 2025-01-01 的 0:00 预报：当天 0:00–1:00 无预报结点（需前一日的预报） | 用前一日的 0:00 预报第 24 个值作为 0:00 结点；2025-01-01 用 0（夜间） |

---

## 三、与工作区约定的关系

本台账是 `AGENTS.md` §六 的执行细则。与 `AGENTS.md`、`roles/*` 冲突时：

1. **赛题本身的规定**（`C题.pdf`、`附件/`）优先级最高；
2. **用户在当前对话中的直接要求**次之（本台账的"已裁决"条目即由此而来）；
3. 再次为本台账；
4. 最后才是 `AGENTS.md` 与 `roles/` 的通用约定。
