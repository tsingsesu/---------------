# 符号表（全工作区唯一，所有 agent 必须遵守）

> 维护人：构思手。规则见 `AGENTS.md` §1.5 与 `roles/构思手.md` §4。
> **新符号第一次使用前必须登记**；同一物理量全工作区只有一个符号；不同问题的同类量优先复用。
> 禁止 `t_first`、`t_final`、`x_1_new`、`temp2` 这类冗长或带序号的下标；一次定义的上标/下标（$t_0$、$t_f$、$x^*$）须在本表说明。
> 代码变量名必须与本表一一对应。

---

## 一、指标与时间

| 符号 | 含义 | 单位 | 量纲 | 取值来源 | 首次出现 |
|---|---|---|---|---|---|
| $k$ | 一天内 10 分钟时段的序号，$k=1,2,\dots,K$；第 $k$ 个时段为 $(10(k-1)\ \text{min},\,10k\ \text{min}]$ | — | — | 定义 | 问题1 |
| $K$ | 一天的时段数，$K=144$ | — | — | $24\,\text{h}/10\,\text{min}$ | 问题1 |
| $\Delta$ | 单个时段长度，$\Delta=1/6$ | h | T | 定义 | 问题1 |
| $d$ | 日期序号，$d=1,\dots,D$；2025-01-01 记作 $d=1$ | — | — | 附件2 | 问题2 |
| $D$ | 仿真天数，$D=365$（2025-01-01 至 12-31） | — | — | 附件2 | 问题2 |
| $D_{\text{rep}}$ | 填报区间天数，$D_{\text{rep}}=334$（2025-02-01 至 12-31，$d=32,\dots,365$） | — | — | 题目/附件5 | 问题2 |
| $\tau$ | 光伏预报发布时刻，$\tau\in\{0,6,12,18\}$（当日小时） | h | T | 附件3 | 问题3 |
| $m$ | 预报提前期，$m=1,\dots,24$；指整点 $(\tau+m){:}00$ 处 | h | T | 附件3 | 问题3 |
| $t_0,\,t_f$ | 一天的首、末时刻，$t_0=0{:}00$，$t_f=24{:}00$ | — | — | 定义 | 问题1 |
| $\mathcal{S}$ | 题目指定的四个填报日期 $\{2025.3.20,\ 2025.6.21,\ 2025.9.23,\ 2025.12.21\}$ | — | — | 题目 | 问题2 |
| $\mathcal{T}_\tau$ | 由 $\tau$ 时刻（或其前最晚一次）调整所覆盖的时段集合 | — | — | 定义 | 问题3 |
| $\mathcal{F}_\tau$ | 到 $\tau$ 时刻为止可获得的预报信息集（$\mathcal{F}_0\subset\mathcal{F}_6\subset\mathcal{F}_{12}\subset\mathcal{F}_{18}$） | — | — | 定义 | 问题3 |

## 二、输入量（题目给定）

| 符号 | 含义 | 单位 | 量纲 | 取值来源 | 首次出现 |
|---|---|---|---|---|---|
| $p_k^{d}$ | 外网电价；问题1/2/3 中 $p_k^{d}=p_k$（逐日相同，取自附件1）；问题4 取自附件4 | 元/kWh | — | 附件1 / 附件4 | 问题1 |
| $L_k^{d}$ | 小区负载功率；问题1 取附件1 负载列（典型日），问题2/3/4 取附件2 | kW | — | 附件1（典型日）/ 附件2 | 问题1 |
| $P_k^{d}$ | 光伏出力功率（**实际**）；问题1 取附件1 光伏预测列（典型日），问题2/3/4 取附件2 `光伏发电实际功率`；问题3 计划阶段的预报量另见 $\hat P_{m}^{(d,\tau)}$ | kW | — | 附件1（典型日）/ 附件2 | 问题1 |
| $\hat P_{m}^{(d,\tau)}$ | 由 $\tau$ 时刻预报给出的、整点 $(\tau+m){:}00$ 处的光伏出力 | kW | — | 附件3 `预报m小时` | 问题3 |
| $\hat P_{k}^{d,\tau}$ | 由 $\tau$ 时刻预报**分解到 10 分钟粒度**后的光伏出力估计（$\sum$ 与 $\hat P_{m}^{(d,\tau)}$ 按小时配比一致） | kW | — | 附件3 + 分解规则 | 问题3 |
| $a_d$ | 逐日乘性因子（分析量：$p_k^d\approx a_d\,p_k$，$L_k^d\approx a_d\,L_k$） | — | — | 由附件1/2/4 反演 | 问题4 |
| $E_0$ | 储能初始储电量（2025-01-01 0:00），$E_0=6000$ | kWh | — | 附录1 | 问题1 |
| $C$ | 储能额定容量，$C=12000$ | kWh | — | 附录1 | 问题1 |
| $\bar P$ | 储能最大充、放电功率，$\bar P=5000$ | kW | — | 附录1 | 问题1 |
| $\underline E,\ \bar E$ | 储电量允许下、上限，$\underline E=1200$，$\bar E=10800$ | kWh | — | 附录1 | 问题1 |
| $\eta$ | 单向充/放电效率，$\eta=0.9$（往返效率 $\eta^2=0.81$） | — | — | 附录1（口径见 题目解读 §7 C-4） | 问题1 |
| $\kappa$ | 紧急购电电价倍数，$\kappa=5$ | — | — | 题目 | 问题2 |
| $\kappa_-$ | 计划量高于调整量部分的违约电价系数，$\kappa_-=0.5$ | — | — | 题目 | 问题3 |
| $\kappa_+$ | 调整量高于计划量部分的超用电价系数，$\kappa_+=1.5$ | — | — | 题目 | 问题3 |

## 三、决策变量与状态

| 符号 | 含义 | 单位 | 量纲 | 取值来源 | 首次出现 |
|---|---|---|---|---|---|
| $x_k^{d}$ | **计划购电量**（当日 0:00 制定） | kWh | — | 决策变量 | 问题1 |
| $y_k^{d}$ | **调整购电量**（该时段最终生效的购电量总量；问题1/2 中 $y_k^d\equiv x_k^d$） | kWh | — | 决策变量 | 问题3 |
| $u_k^{d}$ | 储能充电量 | kWh | — | 决策变量 | 问题1 |
| $v_k^{d}$ | 储能放电量 | kWh | — | 决策变量 | 问题1 |
| $E_k^{d}$ | 时段 $k$ 结束时的储电量（状态变量） | kWh | — | 状态 | 问题1 |
| $r_k^{d}$ | 紧急购电量（供给侧缺额，由约束导出，非独立优化对象） | kWh | — | 决策变量 | 问题2 |
| $g_k^{d}$ | 弃光电量（光伏超过可用部分而被削减的量） | kWh | — | 松弛变量 | 问题1 |

## 四、目标函数与派生量

| 符号 | 含义 | 单位 | 量纲 | 取值来源 | 首次出现 |
|---|---|---|---|---|---|
| $J$ | 总购电费用（问题1 为全天；问题2/3/4 为全天或全期） | 元 | — | 目标函数 | 问题1 |
| $J_{\text{plan}}$ | 计划购电费用，$J_{\text{plan}}=\sum_k p_k x_k$ | 元 | — | 目标分解 | 问题2 |
| $J_{\text{emg}}$ | 紧急购电费用，$J_{\text{emg}}=\kappa\sum_k p_k r_k$ | 元 | — | 目标分解 | 问题2 |
| $J_{\text{adj}}$ | 调整购电量的相关费用（**有符号**：欠取为负、超用为正），$J_{\text{adj}}=\sum_k\big[-\kappa_- p_k (x_k-y_k)^{+}+\kappa_+ p_k (y_k-x_k)^{+}\big]$ | 元 | — | 目标分解 | 问题3 |
| $V_E$ | 终端储电量的单位余值（问题4 终端条件用） | 元/kWh | — | 由次日价格反演 | 问题4 |
| $e_k^{d}$ | 时段 $k$ 的净负荷电量，$e_k=\big(L_k\Delta-P_k\Delta\big)$ | kWh | — | 派生 | 问题1 |
| $\xi_k^{(d,\tau)}$ | 光伏预报的相对误差，$\xi=(P-\hat P)/\max(\hat P,\epsilon_0)$ | — | — | 由附件2/3 反演 | 问题3 |

**总费用的等价写法（口径见 `口径与假设台账.md` D-12，已由用户确认）**：

$$J=J_{\text{plan}}+J_{\text{adj}}+J_{\text{emg}}=\sum_k\Big[p_k\min(x_k,y_k)+\kappa_-p_k(x_k-y_k)^{+}+\kappa_+p_k(y_k-x_k)^{+}\Big]+\kappa\sum_k p_k r_k$$

当 $y_k=x_k$（不做调整）时 $J=\sum_k p_kx_k$，与问题 2"购电费用均按计划购电量计算"**严格一致**——这是该口径被采纳的关键理由，也是问题 2 与问题 3 之间的衔接证据。

## 五、命名与排版约定

1. **上标/下标只用一层含义**：上标 $d$ 一律是日期，上标 $\tau$ 一律是预报发布时刻，下标 $k$ 一律是时段，下标 $m$ 一律是预报提前期。不再引入第二种含义。
2. **省略规则**：当某问题中不含日期维度（问题1）或价格逐日相同时，允许省略上标 $d$（写作 $x_k$、$p_k$），但代码变量名中必须保留完整维度（如 `x_plan[d, k]`）以免混淆。
3. **代码变量名对照**（编程手必须照此命名，新增变量须先在本表登记）：

| 符号 | python 变量名 | 说明 |
|---|---|---|
| $k$ | `k` / 数组末维 | 时段索引 0..143（代码从 0 起，与符号 $k=1..144$ 差 1，须在文件头注释说明） |
| $d$ | `d` / 数组首维 | 日期索引 0..364 |
| $p$ | `price` | 电价矩阵 `(D, K)`，元/kWh |
| $L$ | `load` | 负载功率 `(D, K)`，kW |
| $P$ | `pv_actual` | 光伏实际功率 `(D, K)`，kW |
| $\hat P_{m}^{(d,\tau)}$ | `pv_fc` | 预报 `(D, 4, 24)`，kW |
| $\hat P_{k}^{d,\tau}$（分解后） | `pv_fc144` | 分解到 144 段的预报 `(D, 4, K)`，kW |
| $x$ | `x_plan` | 计划购电量 `(D, K)`，kWh |
| $y$ | `y_adj` | 调整购电量 `(D, K)`，kWh |
| $(x_k-y_k)^{+}$ | `d_under` | 欠取部分（计划高于调整），kWh（问题3） |
| $(y_k-x_k)^{+}$ | `d_over` | 超用部分（调整高于计划），kWh（问题3） |
| $u$ | `u_chg` | 充电量 `(D, K)`，kWh |
| $v$ | `v_dis` | 放电量 `(D, K)`，kWh |
| $E$ | `E_soc` | 储电量 `(D, K+1)`，kWh |
| $r$ | `r_emg` | 紧急购电量 `(D, K)`，kWh |
| $g$ | `g_curt` | 弃光电量 `(D, K)`，kWh |
| $\eta$ | `ETA` | 常量 0.9 |
| $\Delta$ | `DT_H` | 常量 1/6（h） |
| $\bar P$ | `P_MAX` | 常量 5000（kW） |
| $\underline E,\bar E$ | `E_MIN`,`E_MAX` | 常量 1200 / 10800（kWh） |
| $C$ | `E_CAP` | 常量 12000（kWh） |
| $E_0$ | `E_INIT` | 常量 6000（kWh） |
| $\kappa,\kappa_-,\kappa_+$ | `KAPPA_EMG`,`KAPPA_UNDER`,`KAPPA_OVER` | 常量 5 / 0.5 / 1.5 |

**工程函数名（问题 3 新增，登记备查）**：`lib/solve_day.py` 的 `solve_stage`（单阶段 LP，支持边际偏差目标）、`settle_deviation` / `settle_total`（按 D-12 结算）；`lib/run_days.py` 的 `solve_rolling_staged`（多阶段串联滚动、不可追溯）；`lib/forecast.py`（预报读取与分解）；`lib/xlsxio.py`（结果写出）；`lib/logio.py`（日志）。

4. **禁止**：任何未登记的新符号；同一物理量出现两个名字；把 kW 与 kWh 混用（所有电量符号的单位必须是 kWh，所有功率符号的单位必须是 kW）。

5. **论文"符号说明"的呈现规则（用户 2026-09 定，见 `口径与假设台账.md` D-18）**：
   - 论文的"符号说明"表**只列主符号**（如 $J$、$p$、$L$、$P$、$x$、$y$、$u$、$v$、$E$、$r$、$\eta$、$\Delta$、$\bar P$、$\underline E$、$\bar E$、$C$、$E_0$、$\kappa$、$\kappa_-$、$\kappa_+$ 等），**不把 $J_{\text{plan}}$、$J_{\text{adj}}$、$J_{\text{emg}}$ 这类分项记号塞进主表**——它们是 $J$ 的组成部分，不是独立物理量，堆在表里会让符号表臃肿。
   - **但每个分项记号必须在公式中首次出现处就地定义**，例如紧接公式写"其中 $J_{\text{plan}}=\sum_k p_kx_k$ 为计划购电费用、$J_{\text{adj}}=\cdots$ 为调整购电量的相关费用"。**绝不允许出现公式里用了、全文没有定义、符号表里也查不到的记号**（外部审计与评委都会把它当作未定义符号）。
   - 同理适用于所有"由主符号派生、含义由下标自解释"的记号（如 $J^d$、$x_k^d$、$E_{144}^{d}$）；凡是**不能由下标自解释**的，一律回到主表登记。
   - 本节规则只约束**论文的呈现**；代码变量名仍按 §五.3 的对照表执行，本表也仍是全工作区符号的唯一登记处。

---

## 六、待登记 / 待合并（构思手维护区）

- 暂无。任何子 agent 需要新符号时，向构思手申请后由构思手登记进本表。
