"""工作区公共代码层（四问共用）。

模块划分：
  * `dataio`    —— 读取 `附件/` 下四份数据，统一成 numpy 数组（只读，不写回附件）；
  * `timegrid`  —— 10 分钟时段的时间口径、k 与钟点互转、填法 Y-轮转映射（D-01）；
  * `storage`   —— 储能状态方程、储电量边界与充放电功率约束的参数化构造；
  * `solve_day` —— 单日确定性 LP 求解器（问题 1 主模型；问题 2/3/4 直接复用）。

用法：脚本头部把工作区根目录加入 sys.path 后，`from lib.dataio import ...` 即可。
"""
